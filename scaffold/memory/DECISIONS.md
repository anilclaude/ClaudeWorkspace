# DECISIONS

Every decision Claude could not resolve from the PRD, logged here.

**HOLD** — stop and surface immediately. Security, data integrity, or anything
irreversible where the right answer isn't clear. A wrong guess beats no pause.

**PROCEED** — pick the defensible default, keep building, log it. Reversible
choices (naming, default sort order, copy) where stalling costs more.

Resolved decisions that should bind future work get promoted into
`inputs/tech-stack.md` or the PRD, so they aren't re-litigated next session.

---

| Date | Task | Type | Decision / Question | Status |
|---|---|---|---|---|
| 2026-08-09 | T01 | PROCEED | `loginUserSchema.id` and `LoginSuccessData.user.id` typed as `z.string()` (UUID-shaped) — the login PRD's data-entities table (§4) says "primary key" but not a type. TypeORM + Postgres on this stack defaults to UUID PKs; string keeps the contract engine-agnostic. Binds T02's User entity. | PROCEED |
| 2026-08-09 | T01 | PROCEED | Login error response pins `message` and `code` to literals (`"Email or password is incorrect"` / `"INVALID_CREDENTIALS"`) rather than the loose `apiErrorSchema` (`message: string`) used elsewhere, so AC2's uniform-message requirement is enforced at the contract level, not left to service code discipline. | PROCEED |
| 2026-08-09 | T02 | PROCEED | `email` uniqueness enforced case-insensitively via a hand-written functional unique index (`CREATE UNIQUE INDEX ... ON users (LOWER(email))`) in the migration, rather than the `citext` extension or app-layer lowercasing. Accounts are created out of band by an administrator (no self-service registration in this PRD), so app-layer normalization has no reliable single choke point to live in; a DB-level constraint is the one place guaranteed to see every insert. `citext` was rejected to avoid a shared-database extension change (`platform_db` is shared across every backend service) for a need plain Postgres already covers. The entity intentionally does not declare `unique: true`/`@Index` on `email` — TypeORM decorators only express a plain-column (case-sensitive) index, which would misrepresent what the migration actually enforces. | PROCEED |
| 2026-08-09 | T02 | PROCEED | `id` is `uuid` with `gen_random_uuid()` default (Postgres 13+ built-in, no extension) — matches T01's contract decision (`loginUserSchema.id: z.string()`, UUID-shaped). Columns use explicit snake_case `name:` overrides (`password_hash`, `created_at`, `updated_at`) since no TypeORM naming strategy is configured for this service; this is the first entity, so the convention is set here. | PROCEED |
| 2026-08-09 | T02 | PROCEED | Added `maxWorkers: 1` to `backend/auth/jest.int.config.js`. Each `*.int-spec.ts` file has its own module registry and its own `db.ts` DataSource singleton, and every file's `initTestDb()` runs pending migrations — with the second int-spec file this task adds, default multi-worker Jest let two files race to apply the same pending migration against the one shared test database, and the loser's `CREATE TABLE` failed on a duplicate `pg_type` row. Forcing one worker serializes migration application across files; this was a latent gap in the harness, only exposed once a second int-spec file existed. | PROCEED |
| 2026-08-09 | T03 | PROCEED | `POST /auth/login` returns HTTP 200 for both the AC1 success branch and the AC2 uniform-failure branch (`@HttpCode(HttpStatus.OK)`), rather than e.g. 401 on failure. AC2's uniform-message requirement is meant to stop the response from revealing whether an account exists; a status-code difference between "wrong password" and "unregistered email" would reopen that same leak one layer down even with an identical body. A malformed request body (fails `loginRequestSchema`) still gets 400 — that is a client bug, not a credential outcome, so AC2's uniformity does not extend to it. | PROCEED |
| 2026-08-09 | T03 | PROCEED | No dummy-hash `bcrypt.compare` when the looked-up user doesn't exist — `AuthService.login` only calls `bcrypt.compare` when a row was found, so a not-found lookup returns faster than a found-but-wrong-password lookup. The login PRD §5 explicitly defers "rate limiting / brute-force protection" and timing-attack hardening to a future release ("If this ships to a public network, throttling should be the next PRD rather than a later one"), so equalizing response timing here would be hardening work the PRD itself says isn't in scope yet. AC2's own guarantee (identical message/body/status for both causes) is still fully met. | PROCEED |
| 2026-08-09 | T03 | PROCEED | Session token TTL set to 24h (`jsonwebtoken`'s `expiresIn`) — no AC pins a duration; AC10 only requires the session to survive reload/new tab "until it expires or I sign out." A defensible, reversible default rather than a value derived from the spec. | PROCEED |
| 2026-08-09 | T04 | PROCEED | Session-check mechanism implemented as a pure client-side JWT decode + expiry check (`@app/frontend-core`'s `checkSession`), not a backend `GET /auth/session` endpoint. Considered both: a backend round-trip would add latency on every page load without adding a guarantee the client-side check lacks, since (a) the JWT is self-verifying by expiry (`exp` claim) and this PRD has no revocation/logout/refresh mechanism (§5 out-of-scope) that could make a token "invalid" before it expires, and (b) AC6/AC10 are session-*presence* checks that gate a redirect/render decision, not access control — no protected resource in this PRD is gated on trusting the client's session state; any future protected backend endpoint still verifies the JWT signature itself, server-side. The tradeoff accepted: the client never verifies the HMAC signature (doing so would require shipping `JWT_SECRET` to the browser), so a token with a tampered-but-still-three-segment payload and a future `exp` would read as "valid" client-side. That's acceptable here because nothing sensitive is released on the strength of this check alone — it only decides whether to render the login form or redirect. Reversible, non-security-critical call per B8, logged as PROCEED not HOLD. | PROCEED |
| 2026-08-09 | T04 (planning gap) | HOLD | AC10's text says the session persists "until the session expires or I sign out," but no task in `platform/docs/task-ledger.md` (T01-T14) implements sign-out/logout, and no logout endpoint exists anywhere in `backend/auth`. Found by the reviewer during T04's review, routed here rather than silently fixed or ignored — a task decomposition gap, not a code defect in any task built so far. Needs a planning decision (re-run `/plan` with an added task, or confirm sign-out is intentionally out of scope for this pass and update the PRD's §5 to say so explicitly) before the login PRD is considered fully shipped. User confirmed 2026-08-09: continue building the remaining ledger tasks now, revisit this afterward. | RESOLVED — see 2026-08-10 row below |
| 2026-08-10 | T04 (planning gap resolution) | PROCEED | Resolved the row above: sign-out/logout is confirmed out of scope for this pass of the `login` PRD, not deferred by re-running `/plan`. Added an explicit bullet to `login.md` §5 rather than adding a task, because the only place a sign-out control could live is application chrome (nav/header) on `/dashboard`, and the dashboard itself is already out of scope for this PRD (T07's placeholder-dashboard decision above). Building a logout endpoint/mechanism now would mean building UI for a screen this PRD explicitly doesn't own. AC10 is satisfied going forward by expiry-based persistence alone (already covered by T04's `checkSession` mechanism and T11's persistence task) — the "or I sign out" clause is acknowledged but not built here. No ledger task added; T10/T11 unaffected. Logout is now the dashboard PRD's responsibility, to be picked up when that PRD is planned. | PROCEED |
| 2026-08-09 | T05 (scaffold bug) | PROCEED | Fixed a real, previously-undetected scaffold-level bug found during T05's mandatory wireframe-fidelity check: `platform/frontend/web/src/app/globals.css` only had `@import 'tailwindcss';`, no `@source` directive. Tailwind v4 auto-detects content within the *app's own* directory, but `@app/ui` is a separate workspace package pulled in via `next.config.js`'s `transpilePackages`, not copied into `frontend/web/src`, so Tailwind never scanned it — every utility class used only inside a shared component (`Button`, `Field`, `Card`, `Badge`) was silently absent from the compiled CSS, and those components rendered fully unstyled (transparent background, default black text) despite correct class names in the DOM. This was never caught earlier in the session because the screenshot tool wasn't available in this environment even during the earlier login/dashboard demo work — only DOM structure was verified then, not actual computed styles. Fix: added `@source '../../../libs/ui/src';` to `globals.css`. Verified via computed `getComputedStyle` on the submit button (background changed from `rgba(0,0,0,0)`/black text to the correct dark slate/white text) and by confirming `slate-800`, `slate-200`, `red-600` are now present in the compiled CSS chunk. `pnpm --filter @app/web build` reconfirmed green after the fix. This affects every UI task from T05 onward (and, retroactively, any earlier now-rolled-back demo UI work) — worth being aware this class of bug existed undetected before this task. | PROCEED |
| 2026-08-09 | T05 (rework) | PROCEED | Added a regression test for the reviewer SHOULD-FIX on the "T05 (scaffold bug)" entry above (`frontend/web/src/app/globals.css.test.ts`). Chose a PostCSS-level test (runs the real `postcss` + `@tailwindcss/postcss` pipeline directly against `globals.css` and inspects the compiled output) over a full `next build`-based test: it exercises the exact mechanism under test (Tailwind's `@source` resolution) without the much larger and slower surface of a full Next production build, and runs in ~1s as part of the normal `vitest run`. Added `postcss` (`^8.5.26`, matching the version `@tailwindcss/postcss@4.3.3` already resolves internally) as a direct devDependency of `frontend/web` — it was previously only a transitive dependency of `@tailwindcss/postcss`, unresolvable via `require('postcss')` under pnpm's strict linking; this doesn't add a new library, `@tailwindcss/postcss` is already the pinned "PostCSS bridge" in `tech-stack.md`, this only makes an already-present transitive dependency directly resolvable. One correction made while building the test, worth recording: an early draft failed to actually catch the regression — it passed even with the `@source` line deleted from `globals.css` — because the test file itself lives under `frontend/web/src`, which Tailwind's *own* auto content-detection already scans without `@source`, and the target class name (`bg-slate-800`) was spelled out literally in the test's own comments/description. Tailwind's scanner is a plain-text scanner, not JS-aware, so that literal text alone was enough to make Tailwind "see" the class as used regardless of `@source`. Fixed by assembling the target class at runtime (`['bg', 'slate', '800'].join('-')`) so the literal contiguous token never appears anywhere in the file; re-verified manually both ways (temporarily deleting `@source` -> test fails; restored -> test passes) before finalizing. Also added a one-line flag to `tech-stack.md`'s Frontend section noting this `@source`-per-consumer requirement, in case a second UI-shipping package or Tailwind-consuming app is added later. | PROCEED |
| 2026-08-09 | T05 | PROCEED | Login screen visual defaults not pinned by the PRD or `docs/wireframes/login/index.md`: (1) "Forgot password?" link styled `text-sky-700`/`hover:text-sky-800` — no color token exists yet anywhere in the codebase (no tailwind config, no CSS custom properties beyond `@import 'tailwindcss'`), so a distinguishable accent was picked rather than reusing the `Button` primary's slate palette, which would read as non-interactive text. (2) The logo placeholder shrinks at Tailwind's arbitrary `min-[480px]:` breakpoint (`h-12 w-12` below, `h-16 w-16` at/above) to match the wireframes' stated "below 480px ... the logo shrinks" responsive intent (`index.md` "Responsive intent"), since 480px isn't one of Tailwind's default breakpoints. (3) The password field's eye/visibility icon is rendered for wireframe fidelity (B7) but is `aria-hidden`, `pointer-events-none`, and otherwise inert — its actual show/hide behavior, keyboard reachability, and screen-reader state are T11's scope (AC7), not T05's. All three are reversible styling/behavior calls, not spec questions. | PROCEED |
| 2026-08-09 | T06 | PROCEED | Email field wrapped in a real `<form onSubmit noValidate>` (previously the email/password fields sat in a plain `<div>` with an inert `type="submit"` button, per T05's note that submit wiring was deferred). `noValidate` is set so the browser's native `type="email"` constraint-validation UI never fires instead of/alongside AC4/AC5's custom inline messages — the two would otherwise race, and native validation UI can't be styled to match `Field`'s error state. On a passing email, `handleSubmit` clears the error and falls through to a comment marking where T07 wires the actual login API call; T06 makes no network call itself (none existed before this task either), so AC4/AC5's "no network request is sent" is verified with a `fetch` spy that would catch a future accidental call, not left implicit. | PROCEED |
| 2026-08-09 | T06 | PROCEED | Tests use `@testing-library/react`'s `fireEvent` rather than `@testing-library/user-event` — `user-event` is not an installed dependency in `frontend/web` (only `@testing-library/react` + `jest-dom` + `jsdom`, per `tech-stack.md`'s "Vitest + Testing Library + jsdom" pin), and B6 treats adding a dependency as a decision to surface, not a default to reach for mid-task when the already-installed `fireEvent` covers the same interactions (typing into the email field, clicking submit) just as well for this task's assertions. | PROCEED |
| 2026-08-09 | T07 | PROCEED | Session token storage mechanism: `localStorage`, via a new `frontend/web/src/lib/session-storage.ts` (not `@app/frontend-core`, since that package's own header says it must never touch the DOM — mobile also consumes it and would need a different mechanism). AC1 itself is silent on where the token lives; AC10 ("reload or open a new tab ... until it expires or I sign out") is what forces the choice — `sessionStorage` is scoped per-tab and would fail the new-tab half, an in-memory store would fail both. Exports a shared `SESSION_TOKEN_STORAGE_KEY` constant plus `storeSessionToken`/`readSessionToken` so T13 (checkSession on `/login` mount) and T14 (checkSession on `/dashboard` mount + tab focus) read back from the same place T07 writes to. | PROCEED |
| 2026-08-09 | T07 | PROCEED | `frontend/web/src/app/(app)/dashboard/page.tsx` added as a minimal placeholder — no `/dashboard` route existed yet, and AC1 requires `router.push('/dashboard')` to land somewhere real rather than 404. Renders one line of placeholder text via the existing `(app)` route group's `AppShell` layout, following the same pattern as the group's existing scaffold `page.tsx`. The real dashboard is out of scope for the login PRD; `navItems` in `(app)/layout.tsx` is left empty, unchanged. | PROCEED |
| 2026-08-09 | T07 | PROCEED | Added a `@/*` -> `./src` alias to `frontend/web/vitest.config.mts` (`resolve.alias`). Next.js already resolves this alias natively at build time from `tsconfig.json`'s own `paths` entry, but Vite/Vitest does not read `tsconfig.json` paths automatically without an extra plugin (e.g. `vite-tsconfig-paths`, not in `tech-stack.md`) — no test had imported anything through `@/` before T07's `@/lib/services` and `@/lib/session-storage`, so the gap was latent. Chose a plain `resolve.alias` entry (already-available Vite config, no new dependency) over adding a plugin, matching B6. | PROCEED |
| 2026-08-10 | T08 | PROCEED | `login-error.png` also draws an inline red message ("Email or password is incorrect") beneath the password field, in addition to the top banner — but that PNG is a composite illustrating all four error-state ACs at once (`docs/wireframes/login/index.md`'s table: `login-error.png` implements AC2, AC4, AC5, AC8 together), and AC2's own wording only describes a banner ("an error reads ... I stay on /login, and the email field still contains what I typed"), with no field-level message of its own — AC4/AC5 own the inline-error mechanism, with their own distinct copy ("Email is required" / "Enter a valid email address"). Read the drawn inline text under the password field as illustrating the *general* banner+inline-both-can-appear pattern the index.md notes describe, not as a second AC2-specific element to build. Implemented AC2 as the banner only (new `credentialError` state, distinct from `emailError`/`submitError`), matching `index.md`'s explicit "Do not collapse them into one" instruction by keeping the mechanisms separate rather than by rendering both for AC2. Reversible UI-composition call, not a spec ambiguity affecting data/security. | PROCEED |
| 2026-08-09 | T07 (regression) | PROCEED | Fixed a real, pre-existing bug in `frontend/web/src/lib/services.ts`, found live during T07's own AC1 verification (see `docs/build-trace.md`, `orchestrator:live_verification`): `required()` read the env var via `process.env[name]` — a *computed* (bracket) member access. Next.js only inlines `NEXT_PUBLIC_*` vars into the client bundle for *static* `process.env.X` reads (a literal, AST-level text substitution the bundler performs at build time — there's no real `process.env` object in the browser); a computed read isn't recognizable as that pattern, so it silently resolved to `undefined` in the browser even though `.env` had `NEXT_PUBLIC_AUTH_SERVICE_URL` set correctly and the backend itself worked (confirmed via raw `fetch`, 200 OK). This is the same class of bug fixed once already during now-rolled-back demo work earlier in the session; it never carried forward because no task in the login PRD's ledger touches `services.ts` directly — T07 is the first task to exercise it via a live network call, which is exactly why it surfaced only now. Fix: `required()` now takes the already-read value as a parameter (`required(value, name)`), and both call sites read via static `process.env.NEXT_PUBLIC_AUTH_SERVICE_URL` / `process.env.NEXT_PUBLIC_CORE_SERVICE_URL` expressions, matching the pattern Next.js's inliner recognizes. Regression test added at `frontend/web/src/lib/services.test.ts` — the process gap flagged after the same class of issue happened with `globals.css` in T05 (a live fix should go through the builder loop with a test, not just get fixed and moved on from). Test approach: a pure jsdom/unit test against `required()` alone cannot catch this — jsdom's `process.env` is a real, live object, so both static and computed reads work identically under jsdom, which is exactly why the bug shipped past every prior test undetected. A full `next build` + grep of `.next/static` (the most literal analog to T05's `globals.css.test.ts` compiled-artifact approach) was considered and manually verified to work (confirmed `.next/static` does contain the literal `http://localhost:4001` after a real build), but rejected as the test's actual mechanism for the same reason T05's decision rejected it for CSS: ~13s per run is too slow to pay on every `vitest run`. Instead, the test runs the *actual* `services.ts` source text through `esbuild`'s `transformSync` with a `define` for `NEXT_PUBLIC_AUTH_SERVICE_URL` — the same class of static member-expression substitution mechanism Next's own bundler (webpack/Turbopack) uses for this exact inlining — and asserts the real configured URL literal appears in the output; a revert to `process.env[name]` leaves that expression un-replaced and the assertion fails. Manually verified both directions before finalizing: temporarily reverted `services.ts` to the buggy `process.env[name]` form, ran the test, confirmed it failed red with the exact un-inlined source in the diff output; restored the fix via `git apply` of a saved diff, reran, confirmed green. A second, minimal test case documents the contrast directly (a synthetic `process.env[name]` snippet through the same `define`, asserting the value is *not* inlined), so a future change to esbuild's own `define` semantics would flag itself rather than silently invalidating the primary test's assumption. `esbuild` (`^0.27.7`) added as a direct devDependency of `frontend/web` — not a new library per B6, it was already resolving transitively as a peer dependency of `vite`/`vitest` (both already pinned in `tech-stack.md`) per `pnpm-lock.yaml`; pnpm's strict linking just didn't expose it for a direct `import` without an explicit entry, same situation as `postcss` in T05's decision above. The new test file also needs a `// @vitest-environment node` override, since `esbuild`'s `transformSync` asserts `new TextEncoder().encode('') instanceof Uint8Array` on load and that's false under this project's project-wide jsdom environment (jsdom's `TextEncoder` returns a `Uint8Array` from a different realm). `pnpm --filter @app/web build/typecheck/lint/test` all reconfirmed green after the fix (5 test files, 14 tests). Task's `status` in the ledger deliberately left untouched — that's for the orchestrating session to update after re-verifying AC1 live. | PROCEED |
| 2026-08-10 | T10 | PROCEED | Added a new `IconButton` primitive to `@app/ui` (`frontend/libs/ui/src/components/icon-button.tsx`) rather than either a bare raw `<button>` written directly in `page.tsx` or repurposing the existing `Button` for the password visibility toggle. `Button` is sized/variant-styled for full-width, labelled actions (`h-10 px-4`, a `variant` background, etc.) — making it work as a small, absolutely-positioned, icon-only overlay control inside the password field would mean overriding most of its own defaults (height, padding, background, focus ring) at the one call site that needs it, which is the "awkward repurposing" the ledger note flagged. A bare `<button>` in the page would have worked functionally but is exactly the R6 raw-markup risk B7 calls out, and this shape (icon-only, `aria-pressed`-capable, accessible-name-required) is generic enough to recur (any future close/clear/copy icon control) rather than being truly one-off — so it went into the design system instead of the page. `IconButton` requires a `label` prop (used as `aria-label` unless the caller passes an explicit one) so an icon-only control can never ship without an accessible name by omission. Reversible design-system addition, not a spec ambiguity. | PROCEED |
| 2026-08-10 | T10 | PROCEED | No dedicated render test added for `IconButton` itself inside `@app/ui` — that package currently has no `@testing-library/react`/jsdom test setup (only `vitest` for pure-function tests, e.g. `button.test.ts`'s `buttonVariants` check), and `Button` itself has never had one either; adding a full render-testing stack to `@app/ui` for one new component would be disproportionate to this task's scope (B1) when the component is a thin, already-typed wrapper around a native `<button>`. AC7/AC9 coverage for the toggle's actual rendered behavior (masked-by-default, reveal/re-mask, `aria-pressed`, dynamic `aria-label`, keyboard reachability, Tab order) lives in `frontend/web/src/app/(auth)/login/page.test.tsx`, which already has the full `@testing-library/react` setup and renders the real `IconButton` as used on the login screen — that is the AC-bound integration point B2 actually requires a test at. | PROCEED |
| 2026-08-10 | T10 | PROCEED | Toggle is disabled (`disabled={isLoading}`) while a login request is in flight, matching the two `Field`s' own `disabled={isLoading}` on the same form (T09). Not explicitly required by AC7 or AC9, but leaving it enabled during the read-only/disabled loading state would be an inconsistent affordance (a control the user can still operate while every other field on the form is locked). Reversible UI-consistency default, not a spec question. | PROCEED |
| 2026-08-10 | T10 (rework) | PROCEED | Added `@testing-library/user-event` (`^14.6.1`, resolved `14.6.3`) as a direct devDependency of `frontend/web`, to address review cycle 1's SHOULD-FIX that AC9's Tab-order test only checked static DOM order, not real keyboard traversal. `tech-stack.md` pins "Vitest + Testing Library + jsdom" as the frontend test stack; `user-event` is the standard Testing Library companion package for exactly this class of assertion (its `tab()` computes the next focus target the way a browser does, respecting `tabIndex`, not just source order) — not a new library category, the same situation as T05's `postcss` and T07's `esbuild` additions (already-implied by an existing stack pin, just needed a direct entry). T06 had earlier decided against `user-event` for that task's plain click/type interactions, where the already-installed `fireEvent` covered the same ground just as well — this is a different case: nothing in `fireEvent`/jsdom can simulate real Tab-key focus movement, so the assertion the reviewer asked for is not reachable without it. | PROCEED |
| 2026-08-10 | T08 (reviewer escalation) | OPEN | Reviewer's read of the row above (`2026-08-10 \| T08`) diverges from the builder's: the builder treated `login-error.png`'s red-bordered inputs + duplicate inline text as illustrative composite detail, not a literal AC2 requirement. The reviewer flags this as a genuine, unresolved contradiction between the PNG (which draws the detail) and `docs/wireframes/login/index.md`'s own written guidance ("banner and inline error... intentionally both present... Do not collapse them into one" — arguably implying AC2 should populate *neither* the inline slot nor be assumed illustrative-only). Not escalated as a BLOCKER on T08 — the builder's reading is defensible and T08 clears. Escalating because `login-error.png` is *also* T10's wireframe (AC8, the 5xx banner) — the identical ambiguity will recur there verbatim unless resolved once, here, first. Needs a planning decision before T10 is built: treat the PNG's field-level detail as illustrative (safe to build T10 the same way T08 did), or update `index.md`/the PNG to remove the ambiguity. Same category as the T04 sign-out gap — routed for planning attention, not resolved by rework. | OPEN |
| 2026-08-10 | T11 | PROCEED | The AC6 "checking session" state (rendered while `checkSession(readSessionToken())` resolves, and again briefly while the redirect to `/dashboard` is underway for an authenticated visitor) has no wireframe — `login-default.png` only draws the signed-out form, and neither `login.md` nor `docs/wireframes/login/index.md` describe this state at all. Default taken: a small centered `Loader2` spinner (the same icon `Button`'s own `loading` prop already uses elsewhere on this screen, per T09) plus a screen-reader-only "Checking your session…" label inside a `role="status" aria-live="polite"` wrapper, rather than rendering nothing at all — a blank white page for however long the check/redirect takes would be indistinguishable from the page failing to load. Reversible UI default, not a spec question. | PROCEED |
| 2026-08-10 | T11 | PROCEED | The AC6/AC10 session check runs in `useEffect(() => { ... }, [])` (mount-only), not `useEffect(() => { ... }, [router])`. Found empirically during this task's own gate run: `next/navigation`'s `useRouter()` is mocked in `page.test.tsx` as `() => ({ push: pushMock })`, a fresh object literal every call, so listing `router` in the dependency array made the effect re-fire on the very re-render `setSessionStatus` itself triggers, calling `router.push('/dashboard')` twice for a single authenticated mount (would still work in production, where Next's real `useRouter()` is stable, but is fragile and was actively wrong under this test double). `[]` is also the semantically correct shape regardless: AC6/AC10 ask "is there a valid session right now, once, when this page is reached" — not "reconsider this every time some unrelated reference changes". Not resolved with an `eslint-disable-next-line react-hooks/exhaustive-deps` comment because that rule isn't registered in this repo's `eslint.config.mjs` (the disable comment itself failed lint as an unused-directive/unknown-rule error) — the empty array ships with only a plain code comment explaining the choice. | PROCEED |
| 2026-08-10 | T11 | PROCEED | AC10's testable proof follows the ledger's resolved direction exactly: no `/dashboard` protected-route guard or session-awareness was added to the dashboard placeholder (still out of scope for this PRD — see T07's decision above). AC10 is proved two ways, both without touching `/dashboard`: (1) a pure-function persistence/expiry test at `frontend/web/src/lib/session-storage.test.ts` — `checkSession(readSessionToken())` still valid after a simulated reload/new tab (a second, independent call sharing nothing but the localStorage key with the call that stored the token), and correctly invalid once the token's `exp` has passed even though it is still physically present in storage; (2) a component-level companion in `page.test.tsx` — the same persisted token drives a second, fully independent `LoginPage` mount (after `unmount()`ing the first) to redirect again, showing the AC6 mechanism itself is what "remain signed in" looks like from `/login`'s own perspective on a reload/new tab, with zero React state surviving between the two mounts. | PROCEED |
| 2026-08-10 | T11 (reviewer escalation) | OPEN | `docs/wireframes/login/index.md`'s "States not drawn" section describes a "Signed out by expiry" banner ("Your session expired. Please sign in again.") for a user who lands back on `/login` after an elsewhere-in-app session expiry — this state is not bound to any AC in `login.md`, is not in `task-ledger.md` under any task id, and is not built anywhere on `feature/login`. Not a BLOCKER on T11 (AC6/AC10's literal wording doesn't require it, and it's consistent with T11's own resolved "no `/dashboard` integration" scope) — flagged by the reviewer as the same category of planning gap as the T04 sign-out omission above: something the wireframes describe that no task ever picked up. Needs a planning decision (add a task if a future PRD gives `/dashboard` enough shape to trigger this banner meaningfully, or confirm out of scope and update `index.md`) before the login PRD is considered fully shipped. | OPEN |
| 2026-08-10 | T11 (review cycle 1 rework) | PROCEED | SHOULD-FIX 1: the mount effect's `checkSession(readSessionToken())` call is now wrapped in try/catch, falling back to `setSessionStatus('unauthenticated')` on a thrown error (logged via `console.error`, no PII/token content in the message) rather than leaving `sessionStatus` stuck at `'checking'` forever. Mirrors T07's `storeSessionToken` write-path precedent exactly, just on the read side: a storage failure means "can't confirm a session exists," which is the same outcome as "no session" (fail open to the form), not a reason to strand the user on a loading spinner with no way to reach the sign-in form. Regression test added spying on `Storage.prototype.getItem` directly (not the file-level `@/lib/session-storage` mock, which only wraps `storeSessionToken`) so it exercises the real `readSessionToken` hitting a genuinely thrown error. | PROCEED |
| 2026-08-10 | T11 (review cycle 1 rework) | PROCEED | SHOULD-FIX 2: the AC10 reload/new-tab test now has a second case that overwrites the stored token with an expired one between the first mount's `unmount()` and the second `render()`, asserting `push` is NOT called the second time. The original test (kept, still a valid regression on its own terms) used the identical token for both mounts, so it could not have caught a hypothetical module-level caching bug (session result memoized outside the component rather than genuinely re-derived from storage each mount) — the new test's token swap directly falsifies that scenario. | PROCEED |
| 2026-08-10 | T11 (review cycle 1 rework, NITs) | PROCEED | All three optional NITs addressed since they were cheap: (1) dropped the redundant `aria-live="polite"` from the loading spinner's wrapper — `role="status"` already implies it. (2) Updated `frontend/libs/core/src/session.ts`'s and `frontend/web/src/lib/session-storage.ts`'s header/docstring comments off the stale pre-regroup `T13`/`T14` task ids (now `T11`) and off describing an out-of-scope `/dashboard` mount-time/tab-focus integration that was never built and isn't planned for this PRD. (3) Added a one-line comment on the mount effect's empty dependency array noting `reactStrictMode`'s dev-mode double-invoke will call `router.push` twice for an authenticated visitor in local dev only (no production effect, same root-cause class as the mocked-`useRouter()` double-push bug already documented in detail on the same line). | PROCEED |
| 2026-08-10 | cafe-menu-management (planning) | HOLD | `cafe-menu-management.md` (Build order 2 of 7) states "Depends on: `cafe-access-roles`" (Build order 1 of 7). `cafe-access-roles` has not been planned (no `## cafe-access-roles.md` section in `task-ledger.md`) or built (no `feature/cafe-access-roles` branch; PRD still in `_ACTIVE`, not `_SHIPPED`), and `backend/cafe/` does not exist on disk. Two concrete blockers, not just a soft ordering preference: (1) `cafe-access-roles`' own PRD text says it "establishes the `backend/cafe` service itself — every later café PRD adds modules to it, none of them create it" — `cafe-menu-management`'s backend tasks target `backend/cafe/src/modules/menu/`, a path inside a service that doesn't exist yet. (2) VC-004 ("Adding, editing, or removing a menu item is rejected for any role other than Admin") is explicitly "built on `cafe-access-roles`' guard" — that guard doesn't exist yet, so no task can be sized against it without guessing its shape. Planning `cafe-menu-management` now would mean either silently absorbing `backend/cafe`'s scaffold + guard into this PRD's tasks (contradicting `cafe-access-roles`' own stated ownership) or sizing tasks against a nonexistent dependency. Halted before writing any ledger entries or creating `feature/cafe-menu-management`. Recommend running `/plan` on `cafe-access-roles` first (already flagged build-ready in `STATE.md`, no wireframes needed), then re-running `/plan` on `cafe-menu-management`. | OPEN |
| 2026-08-10 | cafe-access-roles (planning) | PROCEED | AC5 ("every café endpoint explicitly declares which role(s) it requires... even 'logged in only, any role' must be stated, not assumed") is read to apply uniformly to every controller method in `backend/cafe`, including the health-check endpoints T01 scaffolds — not just future business endpoints added by later café PRDs. T03's build-time check (VC-004) enforces this against every controller under `backend/cafe/src/modules/**`, with a new `@Public()` decorator as the explicit "no role required" declaration, rather than leaving `/health`/`/health/live` undecorated and special-casing them out of the check. `health.controller.ts` is expected to carry `@Public()` once T03 lands, so the enforcement mechanism has zero unguarded methods to prove itself against from day one rather than shipping with a known, pre-existing gap. Reversible interpretation of PRD wording, not a spec contradiction. | PROCEED |
| 2026-08-11 | cafe-access-roles T02/T03 (pre-build review) | HOLD | No `role` claim exists anywhere in the system for T02's guard to read. Checked the actual code, not just the PRD text: `feature/login`'s `auth.service.ts` signs `jwt.sign({ sub: user.id, email: user.email }, ...)` — no `role`. `feature/login`'s `User` entity has columns `id`, `email`, `passwordHash`, `createdAt`, `updatedAt` — no `role`. `cafe-access-roles.md` §5 says the token "reuses whatever `backend/auth` already issues — no new token format, no new claims format invented here," but the existing format has no role to reuse, and §7/§5 explicitly rule out a new table to store one ("no new database tables... `cafe_db` never stores a copy of who's on staff"). AC1-AC4 and VC-001-003 all presume a role is already present on the token — none of them can be satisfied as written until something, somewhere, decides how a role gets onto that JWT. This blocks T02 (guard can't read a claim that doesn't exist) and transitively T03 (enforces T02's decorators). T01 (service scaffold) is unaffected and can proceed. Two options, not resolved here: (a) extend `backend/auth`'s `User` entity with a `role` column and include it in the JWT payload at sign time — small, contained change, consistent with "no new claims format" if read as "extend the one format" rather than "invent a second one," but touches a PRD (`login`) already marked done and requires its own task/decision to add; (b) redefine `cafe-access-roles`' own mechanism for associating a role with a caller — contradicts its stated "no new tables" constraint, so less clean. Recommend (a), routed here rather than picked silently, per B8/Escalation — this is a cross-PRD contract gap, not a coding decision. | RESOLVED — see 2026-08-11 T00 rows below |
| 2026-08-11 | cafe-access-roles T02 (pre-build review) | HOLD | Two compounding risks in T02's ledger note, corrected in the ledger directly rather than left for the reviewer to catch after the fact: (1) the note says to "reuse the existing JWT verification, do not reinvent it" — there is no existing verification code anywhere in the repo to reuse. `backend/auth` only ever *signs* tokens (`auth.service.ts`, `feature/login` only); nothing decodes/verifies one server-side yet. Worse, `feature/cafe-access-roles` was branched off `master` (see the OPEN row below), which doesn't even have `auth.service.ts` — only the pre-login health-check skeleton. (2) The only JWT-handling code that *does* exist anywhere in the repo is `frontend/libs/core/src/session.ts`'s `checkSession` — which by design decodes the payload **without verifying the signature**, documented there as "not a security boundary." A builder pattern-matching off that (the only precedent they'd find by grepping) would replicate decode-only trust server-side, where it becomes a real R4 BLOCKER (anyone could forge `role: 'Admin'`). T02's ledger note has been corrected to state explicitly: implement verification from scratch with `jsonwebtoken.verify(token, JWT_SECRET, { algorithms: ['HS256'] })`, do not decode-only, and do not use `checkSession` as a reference pattern. Logged as HOLD (security-adjacent, B8) rather than silently fixed, since it's exactly the kind of gap that should be visible before the builder is dispatched, not found by the reviewer after. | OPEN |
| 2026-08-10 | cafe-access-roles T01 | PROCEED | `scaffold/templates/backend-service/` is stale on CORS — corrected 2026-08-11 after reviewer spot-check: CORS is missing from the template *entirely*, not just the `main.ts` call site as first logged here. Grepping the whole template tree for "CORS" returns zero matches: `src/config/env.schema.ts` has no `CORS_ORIGIN` field, `.env.example` has no `CORS_ORIGIN` line, and `src/main.ts` never calls `app.enableCors(...)`. Both `backend/auth` and `backend/core` have all three (schema field, `.env.example` line, and the `enableCors` call). Followed the two real services (and this task's explicit "CORS enabled" requirement) over the template: `backend/cafe/src/config/env.schema.ts` adds `CORS_ORIGIN`, `backend/cafe/.env.example` adds the matching line, and `backend/cafe/src/main.ts` adds `app.enableCors({ origin: env.CORS_ORIGIN, credentials: true })` — all three matching auth/core verbatim. The template itself was not edited (out of this task's scope; a separate follow-up is tracking the template fix) but should be fixed the next time anyone touches it, per its own README's "keeping these current" note. | PROCEED |
| 2026-08-10 | cafe-access-roles T01 | PROCEED | Built `backend/cafe` to a more minimal shape than `scaffold/templates/backend-service/` provides, matching `backend/core`'s actual (more minimal) shape and this task's explicit enumerated reference-file list (`config/env.schema.ts`, `config/index.ts`, `db/data-source.ts`, `main.ts`, `modules/health/*`, `app.module.ts` — no mention of an integration-test harness). Specifically omitted, all deferred to later tasks: (1) `src/test/db.ts`, `db.int-spec.ts`, `load-test-env.ts`, `jest.int.config.js`, `.env.test`, and the `test:int` package.json script — `backend/core` has none of these either, and with zero entities/migrations in `backend/cafe` yet, an integration harness would only re-prove the scaffold plumbing a second time with nothing service-specific to protect; add it when T02 or a later café PRD's first entity/migration lands. (2) `JWT_SECRET` in `env.schema.ts`/`.env.example` and the `jsonwebtoken`/`bcrypt`/`@types/*` dependencies — `backend/core` has these already, but T02's own ledger note explicitly plans to add `JWT_SECRET` to `backend/cafe`'s env schema when it builds the role guard ("already in `backend/auth`'s env.schema.ts" — meaning the established pattern, not that `backend/cafe` already has it); adding it in T01 would be building ahead of the guard that needs it (B1). (3) An empty `src/common/` directory — neither `backend/auth` nor `backend/core` currently has one on disk despite `repo-structure.md`'s tree showing it; git cannot track an empty directory, and this task explicitly excludes `src/common/guards/` (T02's scope), so no placeholder was created. | PROCEED |
| 2026-08-10 | cafe-access-roles (planning) | OPEN | `platform/`'s default branch (`master`) does not contain the `login` PRD's completed ledger work (`docs/task-ledger.md`'s `## login.md` section, commits `b5920c9`/`3523627`) — that work exists only on `feature/login`, never merged to `master`. Per P5, `feature/cafe-access-roles` was created off `master` (not `feature/login`), as this session was explicitly instructed. Its `docs/task-ledger.md` therefore starts from `master`'s pre-login template state and does not carry `login.md`'s 11 done tasks — nothing was deleted, they were simply never present on `master`'s history. This is a process/repo-hygiene gap, not a planning decision this agent can resolve: `master` never absorbing `feature/login` means every subsequent café-suite branch cut from `master` (per P5) will have the same gap, and `cafe-access-roles.md`'s own ledger entries will likewise be invisible from `master` (and from any future PRD branch cut from it) until someone merges. Merging is explicitly outside this agent's authority (CLAUDE.md non-negotiable #4, planner P5 "never commit, push, merge, or rebase") — surfaced for the human to decide when/whether to merge `feature/login` (and, later, `feature/cafe-access-roles`) into `master`. | OPEN |
| 2026-08-11 | cafe-access-roles T00 (planning resolution) | PROCEED | Resolves the row above ("cafe-access-roles T02/T03 (pre-build review)", 2026-08-11) via option (a) from that entry: added a new prerequisite task, T00, to `platform/docs/task-ledger-cafe-access-roles.md`, positioned before T02 (which depends on it). T00 extends `backend/auth`'s existing `User` entity with a `role` column (new TypeORM migration) and includes it in `AuthService.login`'s `jwt.sign(...)` payload, rather than inventing a second mechanism inside `cafe-access-roles` itself. Option (b) was rejected for the reason already given in the row above — it would contradict PRD §7's "no new database tables... `cafe_db` never stores a copy of who's on staff" constraint. T02's and T03's ledger notes updated to drop the BLOCKED framing and reference T00 as a normal dependency instead — the underlying question is resolved, they were only ever blocked on it being undecided, not on anything intrinsic to their own scope. `git status` confirmed clean on `master` before this edit (P5). | PROCEED |
| 2026-08-11 | cafe-access-roles T00 (planning resolution) | PROCEED | `CafeRole`'s location. T02's original note scoped it as backend/cafe-local ("service-local; promote to `backend/libs/` only if a second service needs the same pattern — not this PRD's call to make"). That trigger has now fired: T00 makes `backend/auth` write the five role values onto the JWT at sign time, and `backend/cafe` (T02) reads and validates them against that same set — a genuine cross-service boundary via the token, not two backend packages independently wanting the same utility code. CLAUDE.md non-negotiable #6 ("a cross-boundary type is added to `shared/contracts` first, then implemented against") governs exactly this case and is more specific than T02's original generic placeholder, which was written before this scenario existed and only anticipated "backend/libs" as the generic next step for backend-to-backend reuse. `backend/libs/*` is for backend-only infrastructure code that never crosses a wire boundary (e.g. `nest-kit`'s exception filter and request-id middleware — both purely local to whichever service imports them); `CafeRole` is a value that must never drift between the service that writes it and the service that reads it, which is precisely the failure mode `shared/contracts` exists to prevent (`repo-structure.md`: "duplicated types drift silently... the drift surfaces in production"), and precisely how this codebase already treats every other auth-boundary type — `LoginRequest`/`LoginResponse`/`LoginUser` already live in `shared/contracts/src/auth`, not `backend/libs`, for the same reason. Decision: `CafeRole` becomes a Zod schema, `cafeRoleSchema = z.enum(['Admin', 'Manager', 'Cashier', 'Waiter', 'Kitchen'])`, plus its inferred `CafeRole` type, added to `shared/contracts/src/auth/index.ts` as part of T00 (owning service = auth, per `contracts/src/index.ts`'s own "one folder per owning service" convention — auth is what signs the claim, `backend/cafe` is only a consumer). T02's ledger note is updated to import `auth.CafeRole`/`auth.cafeRoleSchema` from `@app/contracts` instead of defining a local enum; the guard, decorators, and `@Public()`/`@RequireRoles()` mechanism itself stay backend/cafe-local as originally scoped — only the role vocabulary moved, not the guard. Reversible architectural call, not a spec ambiguity — logged as PROCEED per B8, not escalated as a HOLD, since both candidate locations were technically legal and this is a "which shared-code rule applies" judgment, not an open product/security question. | PROCEED |
| 2026-08-11 | cafe-access-roles T00 (build) | PROCEED | `role varchar(32)` has no DB-level `CHECK` constraint restricting it to the five `cafeRoleSchema` values — validated at the application layer only (Zod at the contract boundary now, T02's guard rejection branch once it lands). Matches how every other column on this table is already validated (no existing column uses a DB enum/check either), and keeps the migration a pure additive `ALTER TABLE ... ADD COLUMN` with no constraint to widen later if a sixth role is ever added — a DB `CHECK` would need its own migration to change, `cafeRoleSchema` does not. `varchar(32)` sized comfortably above the longest current value ("Kitchen", 7 chars) with headroom for a future addition, not tied to today's longest string. Reversible schema-shape call, not a spec question. | PROCEED |
| 2026-08-11 | cafe-access-roles T00 (planning resolution) | PROCEED | Existing-user nullability/default for the new `role` column. No seed or demo-data script exists anywhere in the repo (checked `backend/auth` and the whole of `platform/` for any file/script mentioning "seed" — none found), but `user.entity.ts`'s own documented pattern is that accounts are created out-of-band by an administrator, so any environment could already hold hand-created rows before this migration runs, and nothing in the codebase enumerates or can enumerate them at migration time. Rather than guess a default role for pre-existing accounts (fabricating a business fact — which of the five roles a given already-created account should hold — with no source of truth to derive it from, and no PRD text ranking the five roles by default-worthiness), the `role` column is added nullable, with no default value. A `NULL` role produces the exact same outcome AC4 and T02's guard design already define for an unrecognized role value: rejected (403 territory, or 401 if no token at all), never silently granted anything — fail-closed, not fail-open. This requires no new branch in T02's guard (`NULL` and "value not in the five" both fall into the identical existing rejection path, per T02's note's own AC4 language: "treated as no valid role") and no separate backfill/`UPDATE` statement in the migration — Postgres applies `ADD COLUMN ... NULL` to existing rows without needing one. Assigning an actual role to an account going forward is `backend/auth`'s concern per `cafe-access-roles.md` §8 ("Creating or inviting staff accounts — that's a `backend/auth` concern, not built here") and stays out of scope for T00 itself; T00 only adds the column and the claim-carrying mechanism, not a way to set it through the product. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 | PROCEED | The 401/403 rejection message copy (`"Missing or invalid authentication token"` / `"You do not have permission to perform this action"`) is not pinned anywhere in `cafe-access-roles.md` — only the status codes are (AC1/AC2/AC4). Picked generic, non-revealing strings that deliberately say nothing about *why* (no "token expired" vs "bad signature" distinction on the 401 side, no "wrong role" vs "unrecognized role" distinction on the 403 side) since AC4's whole point is that those two causes must be indistinguishable from the caller's perspective. Reversible copy choice, not a spec question. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 | PROCEED | `RolesGuard` is not wired globally (no `APP_GUARD` provider in `app.module.ts`, no `@UseGuards(RolesGuard)` anywhere yet). This task's scope is the guard/decorator mechanism itself (AC1-AC4), not applying it to a controller — `backend/cafe` currently has exactly one controller (T01's `HealthController`), and per the "cafe-access-roles (planning)" decision above, that controller is deliberately left for T03 to decorate with `@Public()` as part of proving the build-time check. Wiring `RolesGuard` globally in T02, ahead of that decoration existing, would make every health-check request start failing closed (empty allow-list, see the next row) between T02 landing and T03 landing — a live regression on an already-verified endpoint for no AC in this task's own scope. First controller to actually need `@RequireRoles(...)`/`@Public()` (T03's health-controller update, or the first business endpoint in a later café PRD) is also the first place `@UseGuards(RolesGuard)` (or an `APP_GUARD` registration) gets added. All four ACs are proved directly against `RolesGuard.canActivate()` in `roles.guard.spec.ts` instead, using real `@RequireRoles`/`@Public`-decorated methods on a test-local dummy controller class so the real `Reflector` metadata path is exercised, not a hand-mocked one. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 | PROCEED | An endpoint with neither `@RequireRoles(...)` nor `@Public()` metadata is not given its own third branch — `RolesGuard` reads a missing `@RequireRoles` list as `[]` (`?? []`), which naturally rejects every role with the same `ForbiddenException` AC2/AC4 already use, no separate code path needed. This is a runtime backstop only, not this task's enforcement mechanism for AC5/VC-004 ("every controller method... a build-time check fails if one is missing") — that's T03's explicit job. Fail-closed-by-construction here means an endpoint that ships without a decorator (before T03's check exists, or if T03's check is ever bypassed) is inert rather than silently open. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 | PROCEED | `AuthenticatedRequest` (`backend/cafe/src/common/guards/authenticated-request.ts`) is a small structural interface (`{ headers: Record<string, string \| string[] \| undefined>; staffId?: string }`), not `import type { Request } from 'express'` extended with a `staffId` field. Matches the existing convention already set by `backend/libs/nest-kit/src/request-id.middleware.ts` (`ReqLike`/`ResLike`), which the same review implicitly endorsed by shipping — keeps this guard's request shape framework-agnostic rather than pulling in Express's full `Request` surface (and a direct `@types/express` dependency this package doesn't otherwise need) for two fields. Reversible, consistent-with-precedent call, not a spec question. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 (build) | PROCEED | Hit a real `exactOptionalPropertyTypes: true` compile error (`request.staffId = payload.sub` — `payload.sub` is `string \| undefined` per `@types/jsonwebtoken`'s `JwtPayload`, and `staffId?: string` rejects an explicit `undefined` under this tsconfig flag even though the property is optional). Fixed by capturing the verified subject into its own `staffId: string` local right after the `!verified.sub` guard (which already throws on a falsy value), rather than re-reading `payload.sub` at assignment time — the narrowed local carries the non-undefined type all the way to the assignment. Mechanical type-system fix, not a design decision. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 (review cycle 1) | PROCEED | BLOCKER fixed: `RolesGuard.canActivate()` previously resolved `IS_PUBLIC_KEY` and `ROLES_KEY` as two independent `reflector.getAllAndOverride([handler, class])` calls — a class-level `@Public()` combined with one method's `@RequireRoles(...)` let the class-level marker resolve `isPublic=true` and return before the method's own role requirement was ever read, a real authorization bypass through a documented decorator usage pattern (both decorators are typed `MethodDecorator & ClassDecorator`). Fixed by reading both keys off `context.getHandler()` first; if either is present at the method level, the method level is resolved entirely on its own and the class level is never consulted — only falls back to reading both keys off `context.getClass()` when neither decorator was declared on the method itself. Two new regression tests added (the exact bypass scenario, and the legitimate positive case — an undecorated method on an otherwise `@Public()` class still correctly falls back to the class level) directly to `roles.guard.spec.ts`. | PROCEED |
| 2026-08-10 | cafe-access-roles T02 (review cycle 1) | PROCEED | SHOULD-FIX 3 from review cycle 1 (`backend/cafe/.env`'s `POSTGRES_PORT=5434` flagged as inconsistent with a `5433` "convention") investigated and NOT applied — the reviewer's finding was checked against the committed `.env.example` templates (which do say `5433`) but not against the actual running infrastructure or the sibling services' own real, gitignored `.env` files. Verified directly: `docker ps` shows the real `platform-postgres` container published as `0.0.0.0:5434->5432/tcp`; the workspace-root `.env` that `docker-compose.yml` actually reads sets `POSTGRES_PORT=5434` (overriding the compose file's own `5433` default); and `backend/auth/.env` and `backend/core/.env` — the two sibling services whose "convention" was being cited — both already use `POSTGRES_PORT=5434` themselves, not `5433`. `backend/cafe/.env`'s `5434` therefore already matches every real local `.env` and the actual container port; changing it to `5433` would have broken `backend/cafe`'s ability to connect to the local Postgres instance, the opposite of the "local-dev correctness" the SHOULD-FIX was aiming for. `5433` only exists in this repo's checked-in `.env.example` template default (correct as a generic template baseline for a fresh clone) and was never live anywhere in this particular working environment. Left `backend/cafe/.env`, `backend/auth/.env`, and `backend/core/.env` untouched; `.env.example` files (which do say `5433`, matching the template default and `docker-compose.yml`'s own fallback) also left untouched, since they were never the inconsistent side. | PROCEED |
| 2026-08-10 | cafe-access-roles T03 | PROCEED | Mechanism chosen: a custom ESLint rule (`backend/cafe/src/common/lint-rules/require-role-policy.cjs`, wired into the root `eslint.config.mjs`, scoped to `backend/cafe/src/modules/**/*.controller.ts`), over a Jest/ts-morph scan test. The ledger note recommended the ESLint approach for durability and this build confirmed the concrete reason: it needs zero new runtime dependency (`typescript-eslint`'s parser already produces a `Decorator`-bearing AST for every `.controller.ts` file `pnpm lint` already visits; confirmed the exact node shape — `ClassDeclaration.decorators`/`MethodDefinition.decorators`, each a `{ type: 'Decorator', expression: CallExpression }` — by parsing the real `health.controller.ts` with `@typescript-eslint/parser` directly before writing the rule), whereas ts-morph would have been a genuinely new library not in `tech-stack.md`. It also folds into the existing `pnpm lint` gate every future controller automatically inherits, with no separate script anyone has to remember to wire up or extend. Rule logic deliberately mirrors `roles.guard.ts`'s own method-wins-outright/class-is-fallback-only precedence exactly, so "declared" and "enforced" can't silently drift apart from each other. | PROCEED |
| 2026-08-10 | cafe-access-roles T03 | PROCEED | Also flags a method carrying both `@Public()` and `@RequireRoles(...)` at once as its own error (`conflictingPolicy`, distinct message from `missingPolicy`) — the optional forward NIT from T02's review cycle 2. Cheap to add since the rule already walks every HTTP-decorated method's decorator list for the core check; scoped to the same-method case only (not a class-level `@Public()`+`@RequireRoles()` combination), matching the NIT's own wording and keeping the addition tight to what was actually asked (B1). | PROCEED |
| 2026-08-10 | cafe-access-roles T03 (build) | PROCEED | Added `eslint` and `@typescript-eslint/parser` as explicit devDependencies of `backend/cafe/package.json` (both already pinned, at the same version ranges, in `tech-stack.md`/the root `package.json` — not a new library per B6). Neither was a direct dependency before this task; `pnpm --filter @app/cafe-service lint` already worked without it via Node's parent-directory `node_modules` walk-up resolving the root-hoisted `eslint` binary, and every other backend service already relies on that same implicit path for its own `eslint .` script. This task's rule-verification Jest test (`require-role-policy.spec.ts`) needed to `require('eslint')`/`require('@typescript-eslint/parser')` directly from application code (not just a script's `$PATH`), so declaring them explicitly there was the more honest and less fragile choice than continuing to lean on hoisting for a second, less incidental use. | PROCEED |
| 2026-08-10 | cafe-access-roles T03 (build) | PROCEED | Broadened the existing root `eslint.config.mjs` CommonJS-globals block from `files: ['**/*.config.js']` to `files: ['**/*.config.js', '**/*.cjs']`, since the new rule module (`require-role-policy.cjs`) uses `module.exports` and needs the same `module`/`require`/`__dirname`/`process` globals declared, or lint itself fails on the rule file with `no-undef`. Verified via a full `pnpm lint` across all 13 packages (not just `@app/cafe-service`) that this shared-config change doesn't regress any other package — all green, since it only adds allowed globals for files matching a new pattern, never removes or restricts anything already passing. | PROCEED |
| 2026-08-10 | cafe-access-roles T03 (build) | PROCEED | `@typescript-eslint/parser`'s package.json `exports` field isn't resolvable via ESM `import` syntax under this project's `moduleResolution: "Node"` (classic) setting (`tsc` error TS2307, suggesting `node16`/`nodenext`/`bundler` instead) — changing that setting repo-wide was out of scope for this task and risked unrelated regressions. `require-role-policy.spec.ts` uses `require()` for both `@typescript-eslint/parser` and the local `.cjs` rule module instead, with a scoped `// eslint-disable-next-line @typescript-eslint/no-require-imports` on each of the two lines — a narrow, load-bearing exception (this file genuinely needs CommonJS interop to load a package whose ESM export map this project's TS config can't follow), not a general loosening of the no-require-imports rule. | PROCEED |
| 2026-08-11 | cafe-access-roles T03 (review cycle 1) | OPEN | `require-role-policy.cjs`'s `conflictingPolicy` check (T03's forward NIT from T02) only fires for `@Public()`+`@RequireRoles()` stacked on the same *method* — not the same *class*. That's the identical silent-bypass shape as T02's fixed BLOCKER: `roles.guard.ts` falls back to reading `IS_PUBLIC_KEY` off the class only when the method has neither decorator, so a class carrying both `@Public()` and `@RequireRoles()` at once would let `isPublic` win there too, silently. Not currently exploitable — no controller in `backend/cafe` declares any class-level decorator today — and this was an explicit, deliberate scope call (see the T03 rows above: "scoped to the same-method case only... matching the NIT's own wording"), not a silently dropped consideration. `cafe-access-roles` is fully built (T00-T03 all done) with no further tasks to carry this into, so it's logged here directly rather than in a next task's ledger note. Needs a planning decision whenever a future café PRD's controller first uses a class-level `@Public()`/`@RequireRoles()` decorator: extend `conflictingPolicy` to also check the class level at that point, or confirm the runtime guard's behavior (silently prefer `@Public()`) is acceptable and this lint gap is fine to leave. | OPEN |
| 2026-08-11 | cafe-menu-management (planning) | OPEN | AC6 ("categories can be reordered, and the menu screen reflects that order") has no wireframe for the reorder control itself — `platform/docs/wireframes/cafe-menu-management/index.md`'s own "States not drawn" section says so explicitly: "the reordering UI itself (drag handles, up/down controls) isn't wireframed... flagging this as a gap for the builder to raise... rather than silently picking one." Per P2, no task for that specific control is written into `task-ledger-cafe-menu-management.md` — AC6's backend persistence (T07) and the display-reflects-order half (T10) are fully wireframed and planned normally. Needs a wireframe (drag-handle vs. up/down-arrow vs. something else is a real UX choice, not a copy/spacing detail) before the reorder-trigger control itself can be planned. | OPEN |
| 2026-08-11 | cafe-menu-management (planning) | PROCEED | VC-004 ("Adding, editing, or removing a menu item is rejected for any role other than Admin") is read as covering the PATCH availability-toggle endpoint (T06) as this PRD's "removing" mechanism, not a hard delete — US2 frames removal as "mark an item unavailable without deleting it," and no AC or wireframe describes an actual delete action anywhere in this PRD. Reversible interpretation of PRD wording, not a spec contradiction. | PROCEED |
| 2026-08-11 | cafe-menu-management T01 | PROCEED | `shared/contracts/src/cafe/menu.ts` shape decisions: (1) camelCase field names on the wire (`categoryId`, `isAvailable`, `imageUrl`, `sortOrder`, `createdAt`, `updatedAt`) mapping to the PRD's snake_case DB columns — no prior contract in this codebase mixed cases, and camelCase is the established convention for every existing schema (`loginUserSchema`, etc.); the DB-column-to-field mapping is T02's entity/migration concern. (2) `menuCategorySchema` intentionally carries only `id`/`name`/`sortOrder`, no `createdAt`/`updatedAt` — matches PRD §7's `menu_categories` table definition exactly, which lists no timestamp columns for that table (unlike `menu_items`, which does); not adding speculative columns per B1. (3) `CAFE_ROUTES` adds a dedicated `GET /menu/items/available` route rather than a `?available=true` query flag on `GET /menu/items` for T05's "available-items query method" — a separate route is simpler to guard/cache independently and reads unambiguously in the contract; T05 implements against this shape. (4) `imageUrl` (create/update requests and the `MenuItem` response) carries a hosted URL string, not raw file bytes — this PRD has no upload endpoint anywhere in its scope (§5/§8 are silent on one), so the contract assumes the client already has a URL by the time it calls create/update; T08/T09's "Upload photo" control will need to resolve to a URL through some mechanism not yet specified — flagging this as the open half of the assumption for whichever task builds that control. (5) `reorderMenuCategoriesRequestSchema` takes a full ordered array of category ids (`orderedCategoryIds`) rather than a list of `{id, sortOrder}` pairs — can't express gaps/duplicates, and the service just assigns `sortOrder = index`; simpler contract for T07 to implement against. All five reversible, non-security shape calls per B8. | PROCEED |
| 2026-08-11 | cafe-menu-management T01 (review cycle 1) | PROCEED | `MenuItem.price`/`createMenuItemRequestSchema.price` stays a raw `z.number()` — not reworked to a string-encoded decimal or integer-cents representation, per the reviewer's own recommendation to log rather than fix. Accepted tradeoff, consciously: this matches the PRD's `numeric(10,2)` column and is fine for T01-T11's own scope (menu CRUD, no arithmetic on price anywhere in this PRD), but PRD §6 says Order Management (café PRD 4) will take a price snapshot off this exact `MenuItem` shape and very plausibly sum it for order totals — the point where JS float drift (`0.1 + 0.2 !== 0.3`) becomes a real bug, and by then this shape is a published contract multiple tasks already depend on, making it expensive to change. Flagging now so Order Management's planning pass makes a deliberate call (e.g. integer cents, or a decimal string type) rather than inheriting float `price` by default because nobody revisited it. | OPEN |
| 2026-08-11 | cafe-menu-management T01 (review cycle 1) | PROCEED | `updateMenuItemRequestSchema`'s `imageUrl`/`description` are `.optional()` but not `.nullable()`, and `imageUrl` also requires `.url()` format — so no payload shape currently expresses "clear this field" (not even `imageUrl: ''` parses, since it fails `.url()`). Not fixed here per the reviewer's own recommendation: no current AC/VC/wireframe asks for a "remove photo"/"clear description" action, so adding it now would be speculative (B1). Logged because T11 (edit form, reusing Add) may plausibly want a "remove photo" affordance later, and the fix is additive (`.nullable()`) not breaking, so it's cheap to add when/if a task actually needs it — just flagging it's not there yet. | OPEN |
| 2026-08-11 | cafe-menu-management T01 (build) | PROCEED | Found and used a zod v4 (`4.4.3`) behavior not previously exercised anywhere in this codebase's contracts: `z.string()`'s first constructor argument, when passed as a plain string, sets the *base type-error* message (fires when the field is missing/wrong-type — e.g. `undefined`), which is a distinct error path from `.min(1, message)` (fires only when the field is present but fails the length check). A field validated only via `z.string().min(1, MESSAGE)` gave a generic "Invalid input: expected string, received undefined" for a fully-missing key, not `MESSAGE` — caught by this task's own tests (`createMenuItemRequestSchema`'s "rejects a missing name" case initially failed red), not shipped and silently accepted per B3. Fixed by using `z.string(MESSAGE).trim().min(1, MESSAGE)` (base-type message and too-small message set to the same string) on every "required" string field (`name`, `categoryId` on both `createMenuItemRequestSchema` and `createMenuCategoryRequestSchema`) — empirically verified via a standalone Node script against the installed `zod@4.4.3` before applying, not assumed. Worth carrying forward as a pattern for any future contracts task in this codebase needing a uniform "field is required" message regardless of whether the key is absent or empty. | PROCEED |
| 2026-08-11 | cafe-menu-management T02 (build) | PROCEED | `menu_items.category_id`'s FK to `menu_categories.id` is declared `ON DELETE RESTRICT` in the migration. Neither `shared/contracts/src/cafe/menu.ts` nor PRD §7's table definition pins delete behavior, and no task in this ledger (T01-T11) adds a "delete category" endpoint, so this is currently unexercised in any real request path — but the migration still has to declare something. RESTRICT is the only option that can't silently violate AC5 ("every item belongs to exactly one category") if a delete-category endpoint is ever added later: CASCADE would delete live menu items out from under a category delete (data loss with no AC/PRD sign-off), and SET NULL isn't legal against `category_id`'s NOT NULL constraint (and would violate AC5 outright even if it were). Reversible default per B8 — a future task adding category delete can revisit if RESTRICT turns out to be the wrong UX (e.g. wanting to force-reassign items first instead of hard-blocking the delete). | PROCEED |
| 2026-08-11 | cafe-menu-management T02 (build) | PROCEED | `menu_items.price` has no DB-level `CHECK (price > 0)` constraint — VC-001/AC2 ("price must be a positive number") stays an application-layer concern (Zod, `createMenuItemRequestSchema`/`updateMenuItemRequestSchema`, already built in T01), matching the precedent `backend/auth`'s `AddRoleToUsers` migration already set for `users.role` ("no existing column in this table uses a DB-level enum/check either"). Followed for consistency with this codebase's established convention rather than re-litigated as a fresh choice. | PROCEED |
| 2026-08-11 | cafe-menu-management T03 | PROCEED | Both `GET /menu/categories` and `POST /menu/categories` scoped `@RequireRoles('Admin')` only (no Manager/Cashier/Waiter/Kitchen) — not treated as a genuine ambiguity requiring a HOLD, despite the task instructions flagging it as a real fork. Checked before deciding, not guessed: (1) the task title itself is literally "Menu category read/create endpoints (Admin)"; (2) `cafe-menu-management.md`'s three user stories (US1-US3) only ever say "As an Admin"; (3) `platform/docs/wireframes/cafe-menu-management/index.md`'s "Responsive intent" section states outright this is "Desktop-first — this is an internal admin screen, not customer-facing," and all three of this PRD's wireframed screens are admin-only; (4) no task in this ledger (T01-T11) builds a non-admin read of categories — a future ordering/kitchen-display screen would belong to a later café PRD (Order Management, PRD 4) with its own planning pass, own AC, and quite possibly its own read endpoint/shape, not this task's to pre-guess. `cafe-access-roles.md` does list Manager alongside Admin for some future sensitive actions ("discounts, cancellations, stock adjustments"), but never for menu management specifically, so Manager was deliberately not added here without a PRD line asking for it (B1 — no speculative scope). Reversible: widening the allow-list later (e.g. Manager) is a one-line change to two `@RequireRoles(...)` calls, not a breaking one. | PROCEED |
| 2026-08-11 | cafe-menu-management T02 (build) | PROCEED | `MenuItem.price`'s TypeORM column is `numeric(10,2)` with no `transformer` — `pg`'s driver returns `numeric` columns as strings by default (a well-known driver behavior, not a bug), so `price` typed `number` on the entity is technically inaccurate for values read back from the database (an insert with a JS `number` works fine; a `SELECT`/reload returns a numeric-looking string). Not fixed in T02 because this task has no read path of its own to prove a transformer against (no controller/service reads this column yet) — deferred to T04/T05/T06, the first tasks that actually query `MenuItem` back through a service method, where the right fix (a `ColumnNumericTransformer` or explicit `Number(...)` at the read boundary) can be chosen and tested against a real call site instead of speculatively. T02's own integration test compares `Number(reloaded.price)` rather than asserting a strict type, to avoid encoding a wrong assumption either way. | OPEN |
| 2026-08-11 | cafe-menu-management T03 (review cycle 1) | PROCEED | SHOULD-FIX, logged not fixed now: `MenuCategoryService.create()`'s `sortOrder = await this.categoriesRepository.count()` is a read-then-write race, not atomic. Two concurrent `POST /menu/categories` requests can both read the same `count()` before either `save()` commits, and each persists a category with the identical `sortOrder` — the reviewer traced this precisely and flagged that the duplicate is **not transient**: unlike a mid-reorder race (already documented on `menu-category.entity.ts`'s `sortOrder` comment from T02, a different mechanism), nothing rewrites these rows again until an admin explicitly triggers T07's reorder endpoint, which overwrites every row's `sortOrder` wholesale. The service's own comment was corrected in the same rework pass to describe this accurately (previously miscategorized, by inaccurate proximity to T02's entity-level comment, as a transient mid-reorder-only concern). Accepted as a known, low-risk gap: this is a single-admin internal tool, no AC/VC anywhere in this PRD states a concurrency requirement, and the category-creation UI (T08 is items, not categories — no dedicated "add category" screen is even wireframed in this PRD) makes a genuine concurrent double-submit an unlikely real-world path. Close this when T07 (reorder) lands — either a DB-computed `MAX(sort_order)+1` read inside the same transaction as the insert, or a unique constraint on `sortOrder` with retry-on-conflict, whichever T07's own design favors. | OPEN |
| 2026-08-11 | cafe-menu-management T04 (build) | PROCEED | AC5 ("every item belongs to exactly one category") — `categoryId` must reference a real category. `menu_items.category_id` already carries a real Postgres FK (`REFERENCES menu_categories(id)`, added in T02's migration), which already rejects a dangling `categoryId` at the database level — proven live by T02's own `menu-item.entity.int-spec.ts` ("rejects an item referencing a nonexistent category"). That alone would satisfy AC5's *data-integrity* half, but an uncaught FK violation surfaces to NestJS as a raw `QueryFailedError` -> unhandled 500, not a clean, named 400 the way VC-002 already expects for the sibling "missing required field" case one line above it in the same AC. Genuine judgment call (task instructions flagged this explicitly, not pinned by any AC/VC/wireframe — T04 has no wireframe at all): added an explicit `categoriesRepository.exists({ where: { id } })` check in `MenuItemService.create()` before the insert, throwing `BadRequestException(MENU_ITEM_CATEGORY_NOT_FOUND_MESSAGE)`. Kept the DB FK in place too, not removed — the app-level check is a UX improvement on top of the existing guarantee, not a replacement for it (defense in depth; a direct `save()` bypassing the service would still be caught by the FK). Added a new contract message constant, `MENU_ITEM_CATEGORY_NOT_FOUND_MESSAGE = 'Category not found'`, distinct from the existing `MENU_ITEM_CATEGORY_REQUIRED_MESSAGE = 'Category is required'` (missing/empty categoryId, VC-002) — a nonexistent-but-present categoryId is a different failure mode a client should be able to tell apart (e.g. "you didn't pick a category" vs. "the category you picked was deleted between page-load and submit"), so reusing the REQUIRED message would have been a defensible but strictly worse alternative. Contracts-first per non-negotiable #6: the new message was added to `shared/contracts/src/cafe/menu.ts` before `MenuItemService`/`MenuItemController` were written against it. | PROCEED |
| 2026-08-11 | cafe-menu-management T04 (build) | PROCEED | Closes the OPEN item logged above ("cafe-menu-management T02 (build)" — `MenuItem.price`'s numeric-as-string driver quirk, explicitly deferred to "T04/T05/T06, the first tasks that actually query `MenuItem` back through a service method"). T04's `MenuItemService.create()` is that first real read path (`.save()`'s `RETURNING` clause returns the row, including `price`, straight from Postgres). Fix chosen: a TypeORM `ValueTransformer` (`numericColumnTransformer`) applied directly on the entity's `price` `@Column`, over an ad hoc `Number(...)` cast in each service method — a column-level fix is a single definition every current and future read path (T04 today, T05's list, T06's update, any later `find`/`findOne`) inherits automatically, rather than a cast that has to be remembered and repeated at every call site touching this column. Verified against real Postgres in `menu-item.service.int-spec.ts` (`typeof item.price === 'number'` and `typeof reloaded.price === 'number'` after a fresh `findOneByOrFail`, not just the JS object passed into `create()`). | RESOLVED |
| 2026-08-11 | cafe-menu-management T05 | PROCEED | Both `GET /menu/items` (admin, all items) and `GET /menu/items/available` scoped `@RequireRoles('Admin')` only — re-checked per this task's explicit instruction to not just assume T03/T04's precedent still applies, specifically for whether "available items" might have a broader/future consumer. Checked: PRD §6 ("Integration across modules") explicitly frames Order Management (café PRD 4, the plausible future consumer of an available-items list for an actual ordering screen) as its own future PRD — "Order Management ... will reference `menu_items` by id ... Menu doesn't need to know Orders exists" — with no task in this ledger (T01-T11) or any currently-planned PRD calling this route from a non-admin flow. `cafe-access-roles.md` lists Manager alongside Admin for some future sensitive actions but never for menu reads. Same reasoning as T03's logged decision above, re-verified rather than copied blind. Reversible: when Order Management is planned, that PRD's own planning pass decides whether `/menu/items/available` gets a widened role list, a second route, or stays as-is for an internal admin-triggered availability preview — not pre-guessed here (B1). | PROCEED |
| 2026-08-11 | cafe-menu-management T05 | PROCEED | Response ordering for both `findAll()`/`findAvailable()`: category display order first (join to `menu_categories`, order by its `sortOrder` — the same column AC6/T07's reorder persists), then each item's `createdAt` ascending within a category, with `item.id` as a final tiebreaker for determinism. Not pinned by any AC/VC/wireframe for T05 itself. Chosen over alphabetical-by-name after checking `cafe-menu-list-default.png` directly: its STARTERS section lists "Spring Rolls" before "Garlic Bread" — not alphabetical — so creation order is the more wireframe-faithful default. Both endpoints share the exact same ordering logic (one private `orderedQuery()` builder method) so a future diff between the two lists (e.g. T06's toggle test) only ever reflects the availability filter, never incidental reordering. T10's own ledger note ("order-as-returned... none is a new endpoint") confirms its list component expects to render items in whatever order this read path returns, with no client-side re-sort — this ordering is what ends up on screen. | PROCEED |
| 2026-08-11 | cafe-menu-management T05 | PROCEED | Neither list response embeds category name/info on each `MenuItem` — the contract (`menuItemSchema`) already carries only `categoryId`, unchanged from T01/T04, and T05 doesn't widen it. T10 (admin list screen, "grouping... category order") is expected to pair this read path with the already-existing `GET /menu/categories` (T03, ordered by `sortOrder`) and group client-side by matching `categoryId`, rather than this task adding a join-and-embed shape with no AC/wireframe asking for one (B1 — no speculative scope). Reversible: if T10 finds this awkward in practice, embedding a `category: { id, name }` object is an additive, non-breaking contract change it can raise at that point. | PROCEED |
| 2026-08-12 | cafe-menu-management T06 (build) | PROCEED | `MenuItemService.update()` re-checks `categoryId` existence (`categoriesRepository.exists({ where: { id } })`, throwing `BadRequestException(MENU_ITEM_CATEGORY_NOT_FOUND_MESSAGE)`) whenever `categoryId` is present in the PATCH body, mirroring `create()`'s check exactly (same message constant, same DB-FK-as-backstop reasoning already logged for T04). AC5 ("every item belongs to exactly one category") doesn't stop applying just because the request is a partial update — `updateMenuItemRequestSchema` can only confirm `categoryId` is a well-formed non-empty string, not that it names a real row, same gap `create()` already had to close. T06's own scope is `isAvailable` only per its title, but `updateMenuItemRequestSchema` (T01) already accepts `categoryId` for T11's fuller edit form, so leaving it unchecked here would let a T11-shaped PATCH silently corrupt AC5 through this same handler. Reversible, non-security data-integrity call per B8. | PROCEED |
| 2026-08-12 | cafe-menu-management T06 (build) | PROCEED | Not-found behavior for `PATCH /menu/items/:id` against an id that doesn't reference an existing item: `NotFoundException(MENU_ITEM_NOT_FOUND_MESSAGE)`, a new contract message constant. PRD/AC/VC text is entirely silent on this case (no wireframe either, T06 is backend-only) — picked the ordinary REST-conventional default (404) rather than e.g. silently no-op-ing or a generic 400, since a PATCH against a nonexistent resource is unambiguously "not found," not a malformed-request or business-rule failure. Reversible default per B8. | PROCEED |
| 2026-08-12 | cafe-menu-management T06 (build) | PROCEED | `test:int` was not run against real Postgres this session despite genuine effort: Docker Desktop was not running at task start (unlike T05, per this task's own instructions), `pnpm db:up`'s prerequisite daemon failed to come up after starting Docker Desktop directly, waiting cumulatively over 10 minutes across several polls, and manually starting the `docker-desktop` WSL2 distro (which did report `Running` afterward) — `docker version`/`docker info` still hung indefinitely against the daemon, suggesting a broken Docker Desktop state in this environment this session, not merely a slow cold start. `pnpm --filter @app/contracts build/typecheck/lint/test` and `pnpm --filter @app/cafe-service build/typecheck/lint/test` are all green (unit coverage: `menu-item.service.spec.ts`'s `update` describe block proves AC3/AC4's field-merge logic, not-found, and categoryId-re-check against mocked repositories). `menu-item.service.int-spec.ts`'s new `update` describe block (AC3/AC4 proven directly against `findAvailable()`/`findAll()` in the same request cycle, mirroring `findAvailable()`'s existing VC-003 precedent, plus not-found/categoryId/partial-update cases) is written and included in the diff but unexecuted — B3 (no test suppression) followed: nothing was deleted, skipped, or weakened to route around this, the gap is disclosed here instead. Whoever next has a working Docker/Postgres locally (the reviewer, or a future session) should run `pnpm test:int` in `backend/cafe` before this task is considered fully verified. **Resolved 2026-08-12**: Docker Desktop's repeated hang/crash root-caused to Windows Defender blocking removal of stale AF_UNIX socket reparse-point files (`dockerInference`, `docker-secrets-engine/engine.sock`) on every startup; cleared both directories fresh immediately before launch, which let the backend start cleanly. `pnpm test:int` now run for real: `@app/cafe-service` — 5 suites, 30 tests, all passed. | RESOLVED |
| 2026-08-12 | cafe-menu-management T07 | PROCEED | `POST /menu/categories/reorder`'s `orderedCategoryIds` must be an exact match for the current set of category ids (same size, no unknown/missing id, no duplicate) or the whole request is rejected with `BadRequestException(MENU_CATEGORY_REORDER_IDS_MISMATCH_MESSAGE)`, a new contract message constant. Not pinned by any AC/VC/wireframe (T07 is backend-only, and neither the PRD nor `reorderMenuCategoriesRequestSchema`'s own T01 comment says what to do on a mismatch). Took the safer of the two options the task instructions raised — reject outright, not a partial/best-effort reorder — because `MenuCategoryService.reorderCategories()` rewrites every row's `sortOrder` wholesale: silently accepting a mismatched set could leave an omitted category's `sortOrder` stale (never rewritten, now inconsistent with whatever the client thinks the order is) or, worse, collide two categories onto the same `sortOrder` if the request contained a duplicate id. Reversible default per B8 — a future task could relax this to a partial reorder if a real use case needs it, but nothing in this PRD asks for that. | PROCEED |
| 2026-08-12 | cafe-menu-management T07 | PROCEED | `MenuCategoryService.reorderCategories()`'s N-row rewrite is wrapped in `this.categoriesRepository.manager.transaction(...)`, with each row's `UPDATE` sequenced via a plain `for` loop inside the callback (not `Promise.all`) — a transaction's `EntityManager` is bound to a single DB connection/query-runner and can't run queries concurrently on it, so `Promise.all` would either serialize anyway or throw depending on the driver. Chosen over the no-transaction alternative (each row's `UPDATE` as its own independent statement) because a mid-write failure — the process crashing, or a later row's update throwing — would otherwise leave `sortOrder` partially rewritten: some categories at their new position, others still at the old one, an inconsistent state with no way to tell which write generation each row is in. A transaction makes the whole rewrite atomic: every row's new `sortOrder` commits together or the transaction rolls back and nothing changes. Reversible, non-security data-integrity call per B8 (the task instructions flagged this as a genuine "your call to make and log, not assumed" fork). | PROCEED |
| 2026-08-12 | cafe-menu-management T07 | MITIGATED | Closes the OPEN gap logged in "cafe-menu-management T03 (review cycle 1)" above (`MenuCategoryService.create()`'s non-atomic `count()`-based `sortOrder` race — two concurrent `POST /menu/categories` requests can persist a duplicate `sortOrder`, and that duplicate is not transient, it "persist[s] in the table indefinitely until an explicit T07 reorder rewrites every row's sortOrder wholesale"). T07's `reorderCategories()` is exactly that rewrite: every category's `sortOrder` is set to its index in `orderedCategoryIds`, a clean contiguous `0..n-1` sequence, whenever an admin triggers a reorder. Closed *in practice*, not by fixing `create()`'s race itself (per this task's own instructions, "your reorder endpoint doesn't need to fix `create()`'s race itself") — the race in `create()` remains theoretically possible on any future concurrent create, but its consequence (a duplicate `sortOrder` persisting indefinitely) is now bounded: the next reorder — the only tool this PRD gives an admin for managing category order — clears it. `create()`'s own comment updated to reflect this (see this task's file-written diff on `menu-category.service.ts`). Status label corrected from an earlier "RESOLVED" (review cycle 1 NIT #1, 2026-08-12): "RESOLVED" overstated this entry's own hedged text — the race in `create()` remains theoretically possible, only its consequence is bounded — so `MITIGATED` is the accurate label; it is not a full fix of `create()`'s race (see the new TOCTOU entry below, same review cycle, for the analogous gap this task's own `reorderCategories()` was found to introduce). | MITIGATED |
| 2026-08-12 | cafe-menu-management T07 | PROCEED | Reorder response is 200 OK (Nest's default for `@Post()` with no `@HttpCode` override), not 201 Created like `POST /menu/categories` (T03's `create()`). Not pinned by any AC/VC/wireframe. Reasoned by analogy to `PATCH /menu/items/:id` (T06), which also stays at the default (200) rather than 201 — `create()`'s 201 signals a new resource was made; `reorder` (like `update`) only mutates existing rows and returns the current list, so 200 is the REST-conventional read/mutate-response code, not the create one. Reversible default per B8. | PROCEED |
| 2026-08-12 | cafe-menu-management T07 | OPEN | `test:int` was not run against real Postgres this session: capped Docker-reachability check (`docker info`, 60s timeout) hit exit code 124 (timed out, daemon unreachable), not retried per the new capped-check rule (no relaunching Docker, no polling). `pnpm --filter @app/contracts build/typecheck/lint/test` (3 suites, 47 tests) and `pnpm --filter @app/cafe-service build/typecheck/lint/test` (8 suites, 94 tests) are all green. `menu-category.service.int-spec.ts`'s new `reorderCategories` describe block (AC6's order-round-trips-through-findAll() proof, plus the two reject cases against real Postgres) is written and included in the diff but unexecuted — B3 followed, nothing deleted/skipped/weakened, gap disclosed here. Same unresolved Docker-daemon issue as T06's identical OPEN entry above (this environment's Docker Desktop/WSL2 state, not a code defect) — whoever next has a working Docker/Postgres locally should run `pnpm test:int` in `backend/cafe` before T06 and T07 are both considered fully verified. **Resolved 2026-08-12**: see the resolution note on "cafe-menu-management T06 (build)" above — same fix, same test run. Both T06's and T07's int-specs are included in the 30 passed `@app/cafe-service` tests. | RESOLVED |
| 2026-08-12 | cafe-menu-management T07 (review cycle 1) | PROCEED | SHOULD-FIX, logged not fixed now: `MenuCategoryService.reorderCategories()` reads the current category id set (`this.categoriesRepository.find()`) to run its exact-match validation *before* opening the transaction that does the actual `sortOrder` rewrite. Between that read and the transaction start, another request — a `create()`, or a second concurrent `reorderCategories()` call — could change the id set, so the validation the reviewer traced runs against a snapshot that may already be stale by write time: a classic TOCTOU (time-of-check-to-time-of-use) gap, since the check and the use are not the same atomic operation. Same *class* of race as the already-logged, accepted `create()` `count()` race above ("cafe-menu-management T03 (review cycle 1)"), and directly adjacent to the "cafe-menu-management T07" `MITIGATED` entry above (that entry bounds `create()`'s race's *consequence* via a reorder; this is a new, distinct race inside the reorder mechanism itself, not the same gap restated). Accepted as a known, low-risk gap for the same reason T03's was: this is a single-admin internal tool, no AC/VC anywhere in `cafe-menu-management.md` states a concurrency requirement, and there is no dedicated multi-admin reorder UI in this PRD (T10 is display-only; the reorder-trigger control itself has no wireframe per this ledger's P2 note) that would make concurrent reorders a realistic path today. Not restructured to close it (e.g. re-reading the id set inside the transaction under a row lock, or a DB-level exact-match constraint) — that would be hardening work beyond this task's bound AC6 scope (B1), consistent with T03's own precedent of logging rather than fixing. Revisit if a future task adds real concurrent-admin usage or a dedicated reorder UI. | OPEN |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | T08/T09 scope split, made explicit per the task instructions' own request: T08 renders the Add Menu Item form's default/empty state only — controlled `name`/`categoryId`/`price` fields (`useState`), the wireframe's required markers (AC1/AC5), and the Save Item/Cancel buttons — with zero submission behavior. Both buttons are `type="button"` (never `type="submit"`), so neither can trigger the form's `submit` event at all; the form's own `onSubmit` handler exists only to call `preventDefault()` (a safety net against native implicit submission, e.g. pressing Enter in a text field), not to run any validation or call any API. T09 owns all of: inline field-level errors, the real `POST /menu/items` call, and rewiring Save Item to `type="submit"` with a real handler. Chosen over wiring Save Item to an inert no-op `onClick` (equally valid per the task's own framing) because `type="button"` is a stronger, structural guarantee of "does nothing yet" than an attached-but-empty handler would be — nothing to forget to remove/replace when T09 lands, T09 just changes the `type` and adds the handler. | PROCEED |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | Category options are not fetched from the real `GET /menu/categories` endpoint in T08 — the `Select` renders only the wireframe's own default: a single disabled placeholder option ("Select category"), matching `cafe-menu-add-default.png` exactly (that PNG shows the placeholder text as the field's current value, not a populated list). This is the legitimate "T08's own scope doesn't need a real fetch yet" alternative the task instructions offered, taken deliberately: T08 is specifically the form's *default/empty* rendering, and an unpopulated select is precisely what that state looks like. Consequence, disclosed per the task's own instruction since it affects every later task calling a guarded café endpoint: `GET /menu/categories` and `POST /menu/items` are both `@RequireRoles('Admin')`-guarded, and `@app/frontend-core`'s `createApiClient`/`Fetcher` (`frontend/libs/core/src/api-client.ts`) still has no way to attach an `Authorization: Bearer <token>` header — nothing was added to it in T08. T09 (real `POST /menu/items` call) and T10/T11 (real `GET /menu/categories` fetch for the dropdown and the list screen) are each blocked on this gap independently and will each need to either extend `Fetcher`'s `init.headers` support end-to-end or add a thin authenticated wrapper — not decided here, since T08 has no real call site to prove either option against. `frontend/web/src/lib/session-storage.ts`'s `readSessionToken()` is confirmed as where the JWT already lives (from the login PRD) for whichever task closes this. | OPEN |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | `cafeService()` added to `frontend/web/src/lib/services.ts` (plus `NEXT_PUBLIC_CAFE_SERVICE_URL=http://localhost:4003` in `.env`/`.env.example`) even though T08 itself makes no real café-service call — done now because every later task in this ledger (T09-T11) needs it, it follows the exact static `process.env.X` pattern `authService`/`coreService` already establish (see that file's own comment on the computed-read bug this guards against, `services.test.ts`, "T07 (regression)"), and a dedicated esbuild-based regression test was added for this new call site specifically rather than assumed to inherit `authService`'s existing coverage for free. | PROCEED |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | A `Select` primitive was added to `@app/ui` (`frontend/libs/ui/src/components/select.tsx`), mirroring `Field`'s exact shape (label/required-marker/error-slot), rather than raw `<select>` markup written directly into `AddMenuItemForm`. `Field`'s own type (`InputHTMLAttributes<HTMLInputElement>`) can't be widened to cover a `<select>`, and this is the first single-select field anywhere in this codebase (AC5's category dropdown). Same reasoning already established for `IconButton`'s addition (login PRD T10) — a generic, recurring shape belongs in the shared library, not the one screen that happened to need it first (T11's edit form reuses this same component). No dedicated `select.test.ts` was added, matching the `IconButton` precedent (also untested standalone) — `Select`'s required-marker and single-select behavior are both exercised through `add-menu-item-form.test.tsx`, the actual AC-bound integration point. | PROCEED |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | The Add Menu Item form omits a Description field entirely, despite `menu_items.description` (PRD §7) being a real, optional column and despite the task instructions' own field list mentioning "description (optional)". Checked directly against both sources this task cites as authoritative: `cafe-menu-add-default.png` (read directly, per B7) draws only Name/Category/Price/Photo — no Description field anywhere on the canvas — and the PRD's own defining user story, US1 ("As an Admin, I can add a menu item with a name, price, category, and photo"), likewise never mentions description. Both the wireframe and the PRD's own product-intent text agree in omitting it from this screen, which is stronger signal than the task instructions' field list (sourced from the DB table alone). B7 ("UI matches what's actually drawn") is read as the controlling policy here over a field list that appears to have been generated from the data model rather than re-checked against the wireframe. `description` stays a valid, unused-by-this-form optional field on `createMenuItemRequestSchema` — nothing here removes it from the contract, only from this screen's rendered fields. Reversible: if a future task finds a real product need for it, adding a `Field` for it is a small additive change. | PROCEED |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | Photo is rendered as an inert, `aria-hidden`, non-interactive dashed placeholder box (`<div>`, matching the wireframe's "Upload photo" dropzone visually) rather than a real `<input type="file">`. This PRD has no upload endpoint and no defined payload shape for a selected file (already logged OPEN, "cafe-menu-management T01") — a real file input that silently drops whatever the user selects would be actively misleading (looks functional, does nothing), which is worse than a control that's visibly inert. Not re-deciding the underlying OPEN item, just choosing how T08 renders around it today. | PROCEED |
| 2026-08-12 | cafe-menu-management T08 | PROCEED | Route is `/menu/new` (`frontend/web/src/app/(app)/menu/new/page.tsx`), not the wireframe's browser-chrome URL `app.example.com/admin/menu/new`. This codebase has no `/admin` path segment anywhere — `/dashboard` (login PRD) is also an authenticated-app-only screen and isn't nested under one either — so `/admin` in the wireframe's illustrative URL bar is read as scene-setting ("this is an admin screen") rather than a literal route spec to reproduce. Reversible naming call, not a spec question. `(app)/layout.tsx`'s `navItems` array is left unchanged (still empty) — a "Menu" nav entry is deferred until T10 builds the list screen, which is the natural link target (`/menu`); linking to `/menu/new` alone, or to `/menu` before it exists, would both be premature. | PROCEED |
| 2026-08-12 | cafe-menu-management (T08 review escalation) | OPEN | T08's Category `<select>` is deliberately left unpopulated (placeholder only) — reasonable for T08's own "default state" scope, and wireframe-supported (the PNG's own AC5 annotation frames the field as a single-vs-multi-select structural claim, not a live-data claim). But no task in the current ledger (T09, T10, T11) is actually bound to fetching real categories into this dropdown, and none is bound to closing the prerequisite gap it depends on: `@app/frontend-core`'s `createApiClient` has no way to attach an `Authorization` header, so no frontend call to any `@RequireRoles('Admin')`-guarded café endpoint (`GET /menu/categories`, `POST /menu/items`, the PATCH/reorder endpoints) can be made for real yet — this would be the first authenticated frontend→backend call in the project. T09 wires the real `POST /menu/items` call (AC1, AC2) but its current binding doesn't include AC5 or a categories fetch. Net effect if nothing changes: once T09 lands, an Admin will be unable to actually save an item through the UI at all, since Category is required and has no way to be populated. Needs a planning decision before T09 is built — either widen T09's scope to include the categories fetch + auth-header plumbing, or add a new task for it, rather than letting T09 discover this cold mid-build. Same category as the login PRD's T04 sign-out gap and T08(reviewer escalation) pattern: a real gap surfaced by review, routed here for planning attention rather than silently absorbed into the next task's scope. | OPEN |
| 2026-08-12 | cafe-menu-management (T08 review escalation resolution) | PROCEED | User decision: build T09 exactly as currently scoped (AC1, AC2 only) — do not widen it to include the auth-header plumbing or a real categories fetch, and do not insert a new prerequisite task. The form will remain functionally unable to actually save an item (Category has no way to be populated) after T09 lands; this is accepted as a known, temporary gap to be closed in a later task rather than fixed now. T09's builder dispatch should not attempt to solve the auth-header gap itself. | PROCEED |
| 2026-08-12 | cafe-menu-management T09 | PROCEED | `categoryId` is validated as "must be non-empty" (new `MENU_ITEM_CATEGORY_REQUIRED_MESSAGE` inline error), even though `cafe-menu-add-error.png` itself only draws Name and Price as erroring (Category shows a filled "Starters" value in that specific drawn state). Per this task's own dispatch — "AC1 does require category to be selected before save, so an empty categoryId should still block submit same as empty name" — the PNG is one illustrative error state, not proof that an empty-category submission is accepted; AC1's text ("requires a name, category, and price") is unambiguous on this point. Not a new UI element beyond what `Select`'s existing `error` prop already renders (same red border/inline-text shape T08 already established), so no wireframe deviation, just a state the PNG didn't happen to draw (B7 — implement it with a sensible default). | PROCEED |
| 2026-08-12 | cafe-menu-management T09 | PROCEED | The real `POST /menu/items` call's failure path (network error, or a thrown `ApiClientError` for a non-2xx response — expected today given the known auth-header gap) surfaces a local, client-owned banner ("Something went wrong saving this item. Try again.") — not drawn on any wireframe for this screen (only the client-validation error state is drawn) and not a `shared/contracts` constant (never crosses the service boundary, same reasoning as the login PRD's `SERVER_ERROR_MESSAGE`). A sensible default per B7's error-state requirement, copying the login PRD's own precedent for an equivalent "the request itself failed" case rather than inventing new banner styling. | PROCEED |
| 2026-08-12 | cafe-menu-management T09 | PROCEED | Save Item passes `loading`/`disabled` to `Button` while the POST is in flight (mirrors the login PRD's `isLoading` pattern) as a plain `useState`-driven default against accidental double submission — not a claimed, tested guard. Deliberately not built to the login PRD's AC3 standard (a `useRef`-based same-tick guard, proven live against a real double-click) since no AC/VC in this PRD's T09 binding asks for that; adding an untested race-condition claim here would repeat the exact gap T09 (login PRD)'s review cycle 1 found and had to rework. If a future task needs a real double-submit guard on this form, treat it as its own bound, tested unit of work rather than assumed from this default. | PROCEED |
| 2026-08-12 | cafe-menu-management (T08 escalation, resolution 2) | PROCEED | User decision: unlike T09, T10 (Admin menu list, AC3/AC5/AC6 display half) cannot defer the auth-header gap — its ACs are specifically about rendering real fetched data (grouped by category, in category order, correct availability badge state), so a static/hardcoded render would prove nothing. T10's scope is widened to include adding `Authorization`-header support to `@app/frontend-core`'s `createApiClient` (and wiring the real `GET /menu/categories`/`GET /menu/items` calls) rather than inserting a separate prerequisite task. This closes the auth-header gap first logged in "cafe-menu-management T08" and re-flagged in "(T08 review escalation)" — T11 (edit form, reuses T08/T09's form) should be able to rely on it being solved by the time it's built. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | `createApiClient`'s new third param is `TokenProvider = string \| (() => string \| null \| undefined)` — a static string or a getter function — rather than a single fixed shape. The getter form is what `cafeService()` actually needs (`readSessionToken` passed by reference, re-read fresh on every request rather than snapshotted once when `cafeService()` is constructed, since a component can hold a `cafeService()` reference across a session change); the string form is kept too since it's the simpler shape for a caller that already has a token in hand and costs nothing extra to support. `@app/frontend-core` itself never reads `localStorage`/any DOM API — only invokes whatever getter it's handed — keeping the package platform-agnostic per its own module header. `null`/`undefined` from either form (or the param omitted entirely) means "no token," never a literal `Bearer null`/`Bearer undefined` header. `authService()`/`coreService()` are untouched — the new param is optional, so their existing two-argument call sites compile and behave identically. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | `cafeService()`'s no-session behavior: no special-casing. `readSessionToken()` returning `null` (no stored JWT) simply means `createApiClient` sends the request with no `Authorization` header, exactly like any other unauthenticated call — the backend's `RolesGuard` then rejects it (401), which is indistinguishable, from this app's fetch call site, from any other non-2xx failure. `MenuListScreen` (this same task) already has to handle a generic fetch failure with a clean error state for other reasons (network errors, contract drift), so no-session rides that same path rather than getting its own "please sign in" branch — this PRD has no sign-in-aware routing/guard anywhere yet (see the login PRD's own logged T07/T11 decisions: no `/dashboard` protected-route guard exists either). Reversible: a future task can add a distinct "not signed in" message once this app actually has a concept of redirecting to `/login`, without changing `cafeService()`'s or `createApiClient`'s shape. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | A category with zero items (per the current `GET /menu/items` response) renders no section/header at all — matches `cafe-menu-list-default.png` exactly (every drawn STARTERS/MAINS/BEVERAGES header has at least one item under it; no empty section is drawn), and isn't pinned by AC3/AC5/AC6's text either way. If every category is empty (or there are no categories/items at all), the result is the same "No menu items yet" empty state index.md's own "Empty state" section specifies, not a page of bare, itemless headers. Reversible UI default per B8. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | Each item row's "Edit" link routes to `/menu/${item.id}/edit` — a route that doesn't exist yet (T11 builds it). Chosen over a `#`/no-op placeholder because a real, guessable, RESTful-shaped href is a reasonable target the moment T11 lands (T11 just needs to add the matching `page.tsx`), matching this task's own instruction that only the href, not the toggle behavior, is this task's concern. Reversible naming call. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | "+ Add Item" (both the header button and the empty-state button) renders as a real `<a>` (`next/link`'s `Link`, styled via `@app/ui`'s exported `buttonVariants` + `cn`) rather than a `<Button onClick={() => router.push(...)}>`. This is plain navigation, not a submission or an action with side effects, so a real anchor is the more correct element (works without JS, right-click/open-in-new-tab, no client-side router dependency) — and `Button` itself renders a native `<button>`, which cannot be nested inside an `<a>` (invalid HTML, interactive content inside interactive content), ruling out wrapping `<Button>` in `<Link>` directly. The existing `Edit` link (T08's `Select`/`Field` precedent has no equivalent) uses the same plain-anchor-with-utility-classes approach for the same reason. Reversible UI-composition call. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | `apiResponseSchema`'s discriminated union types each response's `data` as `T \| null` until `success` is checked (mirrors the login PRD's own uniform-failure-at-200 shape) — `MenuListScreen`'s fetch treats a `success: false` categories/items response (not currently produced by either read endpoint per T05, but a legal shape of the published contract) the same as a thrown fetch error: the generic load-error banner, not a crash or a silent empty list. Reversible, defensive-only branch. | PROCEED |
| 2026-08-12 | cafe-menu-management T10 (build) | PROCEED | Added a "Menu" entry (`{ href: '/menu', label: 'Menu' }`) to `frontend/web/src/app/(app)/layout.tsx`'s `navItems` — T08's own logged decision explicitly deferred this exact entry "until T10 builds the list screen, which is the natural link target." No AC requires app-chrome navigation, but leaving the list screen unreachable from anywhere but a typed-in URL once it exists would be a real usability gap for an admin screen. Reversible UI addition. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | Edit mode is threaded through `add-menu-item-form.tsx` itself via three new optional props (`mode?: 'add' \| 'edit'`, `item?`, `categories?`) rather than a separate wrapper component that reimplements the fields/validation. Every prop defaults to T08/T09's original behavior when omitted, so `/menu/new`'s existing `<AddMenuItemForm />` call site is byte-for-byte unaffected. Chosen because the task's own dispatch explicitly names this file as "the form to reuse in edit mode," and a wrapper-composes-the-existing-component approach would either duplicate the whole field/validation block (defeating the point of reuse) or need the same prop surface passed through an extra layer for no benefit. Reversible naming/shape call. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | No single-item `GET /menu/items/:id` endpoint exists (only `GET /menu/items` and `GET /menu/items/available`). New `EditMenuItemScreen` fetches the full `GET /menu/items` list (plus `GET /menu/categories`, same parallel `Promise.all` T10 already uses) and finds the item client-side by id — the same list-and-find pattern T10 established for categories+items grouping. Pragmatic: needs no backend change, and `GET /menu/items` already returns unavailable items (AC3), so nothing is filtered out that an edit screen would need. A future task could add a real `GET /menu/items/:id` if list size ever makes this wasteful; not needed at current scale. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | The Category `<select>` in edit mode renders real `<option>`s from a `categories` prop (fetched by `EditMenuItemScreen` via `GET /menu/categories`, reusing T10's exact call/TokenProvider plumbing) instead of T08's single disabled placeholder. `/menu/new` (add mode) is deliberately left unchanged — still placeholder-only, per the still-standing T09 decision not to widen that screen's scope. This is treated as a necessary consequence of pre-filling a real `categoryId` into a `<select>` (the task's own dispatch flags this directly), not scope creep beyond AC3/AC4. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | The Available/Unavailable toggle fires its own immediate `PATCH /menu/items/:id` the moment it's clicked — a separate call from the Save Item button's submit, not gated behind the form's own name/category/price validation and not deferred until a later Save. Reasoning: AC4's "at any time" language reads as the admin being able to flip availability on its own, without an unrelated field error blocking it or a stale draft in another field being force-saved as a side effect. Represented as a `Badge` (current state) + secondary `Button` that flips it — `@app/ui` has no toggle-switch primitive and the task's own dispatch left this open; a new switch component felt like unwarranted scope for one screen's one control, so the existing Badge/Button vocabulary was reused instead. Placed below the field grid, above the Save/Cancel row (no wireframe pins a location — the control isn't drawn anywhere). Reversible UI/UX call, logged per B8. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | `AddMenuItemForm`'s `CardTitle` reads "Edit Menu Item" in edit mode rather than the wireframe's literal "Add Menu Item" text. index.md's "reuses `cafe-menu-add-default.png`/`cafe-menu-add-error.png`... layout is identical to Add" is read as a layout instruction, not a mandate to keep the title text unchanged on a screen that isn't adding anything — a title that still says "Add" while editing an existing item would be actively confusing. Reversible copy call. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | `EditMenuItemScreen` treats "fetch succeeded but no item in the list matches the id in the URL" (a stale Edit link, or a hand-edited/bad id) as a distinct `not-found` state from `error` (the fetch itself failing) — its own message ("This menu item could not be found.") plus a "Back to Menu" link, rather than folding it into the generic load-error banner. Neither state is drawn on any wireframe (B7 still requires them); not-found specifically was named in this task's own dispatch as needing coverage. Reversible UI default. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | `frontend/libs/core/src/api-client.ts`'s `ApiClient` interface gains a `patch` method (same `request()` internals as `get`/`post`, method `'PATCH'`) — the first caller in this codebase needing a partial update. `authService()`/`coreService()`/existing `get`/`post` call sites are unaffected (additive-only interface change). Not logged as a new dependency (B6) — no new library, just a new method on an existing in-repo package. | PROCEED |
| 2026-08-12 | cafe-menu-management T11 (build) | PROCEED | `app/(app)/menu/[id]/edit/page.tsx` types `params` as `Promise<{ id: string }>` and awaits it — Next.js 16's App Router convention for dynamic-route params (server components), confirmed against the installed `next@16.3.0` and against `next build`'s own route table, which lists `/menu/[id]/edit` as a real dynamic (`ƒ`) route. First dynamic-segment route in this codebase, so no prior local precedent existed to follow. | PROCEED |
| 2026-08-12 | cafe-menu-management (T11 review cycle 1) | PROCEED | Reviewer SHOULD-FIX, accepted and logged rather than fixed (explicitly framed by the reviewer as non-blocking and low-severity): if an item's `categoryId` no longer matches any entry in the fetched categories list (e.g. its category was deleted — a case `docs/wireframes/cafe-menu-management/index.md` itself flags as "not specified in the PRD, not wireframed"), the edit form's `<select>` displays an inconsistent value while `categoryId` state still holds the stale id. No silent data corruption results — state, not the DOM, drives the submitted PATCH body, so saving without touching Category is a no-op on that field — but the visible Category value could mislead an admin. No category-deletion mechanism exists anywhere in this PRD's ledger (T01-T11), so this gap has no way to actually trigger today; revisit if/when a future PRD adds category deletion. | OPEN |
| 2026-08-13 | cr-dashboard-menu-management-link (planning) | PROCEED | `platform/docs/wireframes/dashboard/CafeDashboard.png` has no `index.md` — every other wireframe folder in this repo (`login`, `cafe-menu-management`) pairs its PNG(s) with one, per `repo-structure.md`'s stated shape and P2's binding requirement. For a CR with exactly one wireframe image and no screen-to-AC ambiguity to resolve (the CR's own ACs already state what's changing), requiring a full `index.md` before planning can proceed would be disproportionate — bound this CR's tasks directly to the PNG instead. Reversible, low-risk per B8; revisit if this feature ever grows a second screen. | PROCEED |
| 2026-08-13 | cr-dashboard-menu-management-link T01 (build) | PROCEED | AC4's opening sentence ("the 'Menu Management' left-nav item is the only real navigation link in the entire left nav") is read literally to include "Dashboard" itself — Dashboard renders as a highlighted/active, non-navigating `disabled` button, not a self-link to `/dashboard`. VC-CR-002's own test wording only requires proving inertness for "any left-nav item other than Dashboard or Menu Management" — read as scoping what the test exercises (clicking the current page is a same-page no-op either way, not worth a dedicated assertion), not as granting Dashboard real-link status; AC4's own unambiguous "only real navigation link" wording governs. Every inert nav item (including Dashboard), plus every other decorative control on the screen (hamburger, search input, notification bell, user-menu button, store selector, welcome-section date picker, table row "view" icons, "View all" on Recent Menu Changes) is implemented as a real `disabled` `<button>`/`<input>` — no `href`/`onClick` exists to navigate from, and `disabled` guarantees no click event fires at all, so VC-CR-002 (no navigation, no console error) holds structurally rather than by omission. | PROCEED |
| 2026-08-13 | cr-dashboard-menu-management-link T01 (build) | PROCEED | Menu Overview's per-category "Active"/"Inactive" status badge (AC3) is derived, not read off a property — `MenuCategory` (`shared/contracts/src/cafe/menu.ts`) carries only `id`/`name`/`sortOrder`, no enabled/active flag of its own. A category renders "Active" (green) if it has at least one available item, "Inactive" (neutral) otherwise. Reversible default per B8, not a spec question — the CR's own wireframe only ever draws "Active" (every row in the sample data happens to have active items). | PROCEED |
| 2026-08-13 | cr-dashboard-menu-management-link T01 (build) | PROCEED | Screen renders inside `(app)/layout.tsx`'s shared `AppShell` (`max-w-6xl`, centered, `px-4 py-8`) rather than breaking out of it or extending `AppShell` itself to support the wireframe's much richer edge-to-edge sidebar — `AppShell` is shared by `/menu` and its sub-routes, which are out of this task's scope, and modifying it would be an unrequested refactor (B1). Result: a width-constrained approximation of `CafeDashboard.png`'s layout/hierarchy, not a pixel-exact full-bleed reproduction, and `AppShell`'s own generic top bar (brand + "Menu" link) renders above this screen's own wireframe-matching header, so two horizontal chrome bars stack rather than one. Reversible layering/sizing default per B8; worth revisiting if/when a later task unifies app chrome across screens. | PROCEED |
| 2026-08-13 | cr-dashboard-menu-management-link (planning) | PROCEED | Scope interpretation of the CR's prose request, since it described the dashboard as if its widgets already exist ("retain all existing widgets and layout") when the actual current `/dashboard` is a one-line placeholder (T07, login PRD, itself still unshipped) — nothing in it currently exists to "retain." Read literally, building everything the wireframe draws pixel-for-pixel would mean: a real activity/audit-log subsystem for "Recent Menu Changes" (no such table, mutation-logging, or endpoint exists anywhere in this codebase) and live routes for Orders/Kitchen Operations/Inventory/Purchase & Suppliers/Staff Management/Attendance & Roster/Customers/Loyalty Program/Reports/Analytics/AI Assistant/Settings (none of these modules exist or are even planned yet) — that's full-PRD-scale work, not a CR. Scoped instead to match this codebase's existing "inert placeholder for infrastructure that doesn't exist yet" precedent (T08's Photo field, T10's category-dropdown-deferred decisions): the three KPI cards and the Menu Overview table render real data, reusing `cafe-menu-management`'s already-shipped `GET /menu/categories`/`GET /menu/items` (the exact same calls T10's `menu-list-screen.tsx` already makes, so this adds no new backend work) — everything else in the wireframe (every nav item but Dashboard/Menu Management, the search bar, notifications bell, user-profile dropdown) renders exactly as drawn but is visually inert, and "Recent Menu Changes" renders an explicit empty/placeholder state rather than fabricated activity data. Also extended the CR's own "one active link" instruction to the wireframe's "+ Add Menu Item" button (same `/menu/new` destination as the Menu Management link) — leaving a primary-CTA-styled button that looks functional but silently does nothing would be more misleading than either wiring it or making it visibly inert, and it's a zero-risk, already-existing route. Because this narrows a literal reading of the request rather than just filling a gap it left silent, the CR was written with `Build gate: yes` — held for the user's explicit confirmation before `/build` runs, rather than auto-chained. | PROCEED |
| 2026-08-13 | cr-dashboard-menu-management-link T01 (review cycle 1 rework) | PROCEED | SHOULD-FIX #1: the four icon-only/decorative controls that were raw `<button aria-label=... disabled>` markup (`TopHeader`'s hamburger, notifications bell, and user-menu button; the Menu Overview table row's "view" icon button) now use `@app/ui`'s `IconButton` primitive, matching the login PRD's password-toggle precedent. `IconButton`'s own default disabled styling (`disabled:opacity-60`) would have visibly dimmed these controls, which the original raw markup never did (only `disabled:cursor-default`, keeping full color/opacity) — overridden per-instance to `disabled:opacity-100` (plus `text-slate-600` on the two icon-only header buttons, matching their original color) to preserve the existing wireframe-matched look, since these controls are decorative placeholders, not genuinely "disabled" affordances a user might expect to re-enable. The user-menu button additionally overrides `IconButton`'s icon-only `h-8 w-8`/`justify-center` sizing to `h-auto w-auto`/`gap-2` to fit its avatar+name+chevron content, the same pattern `IconButton`'s own doc comment anticipates for non-single-icon callers. SHOULD-FIX #2 (AppShell's own top bar double-stacking with this screen's header) and the NIT (fetch-effect duplication vs `menu-list-screen.tsx`) were already disclosed/accepted per the reviewer's own framing and out of scope for this rework — left unchanged. | PROCEED |
| 2026-08-13 | cr-remove-appshell-chrome-from-dashboard (planning) | PROCEED | Scope interpretation of "on the dashboard upper section has two options - 1 Platform, Menu, that section is not required": read as removing `AppShell`'s shared top bar (the "Platform" brand link + "Menu" nav link, `frontend/web/src/components/app-shell.tsx`) specifically from `/dashboard` — not globally from every `(app)` route. This is exactly the double-header deviation the reviewer flagged as SHOULD-FIX #2 on the previous CR ("two horizontal chrome bars appear where the wireframe draws one"), now being explicitly requested rather than deferred. Checked for a navigation dead-end before scoping this narrowly: `add-menu-item-form.tsx`'s "Cancel" button (`/menu/new`) has no `onClick` at all — it's fully inert — so `AppShell`'s "Menu" link is currently the *only* in-app path from `/menu/new` back to `/menu`. Removing it globally would strand a user on `/menu/new`/`/menu/[id]/edit` with no way back except the browser's own back button. Scoping the removal to `/dashboard` only avoids this entirely: `/menu`, `/menu/new`, and `/menu/[id]/edit` keep `AppShell`'s top bar exactly as it is today, and `/dashboard` never needed it in the first place (its own "Menu Management" nav item already covers the CR's stated reasoning). Also noted, not fixed here: `AppShell`'s "Platform" link points at `/`, which has no `page.tsx` at all (a pre-existing 404, unrelated to this CR). **Amends**: `platform/docs/prd/_CHANGE_REQUESTS/cr-dashboard-menu-management-link.md` — its T01 is `done` but the CR itself hasn't been shipped via `/wrap` yet (`Status: open` still), so it can't be cited as a `_SHIPPED/` file per P7's letter; cited directly with that caveat stated rather than forcing an inaccurate citation. Because the "dashboard-only, not global" scoping is a real interpretive call with a discovered dead-end risk behind it, this CR carries `Build gate: yes`, held for the user's confirmation before `/build` runs. | PROCEED |
| 2026-08-13 | cr-remove-appshell-chrome-from-dashboard T01 (build) | PROCEED | Checked for existing precedent of conditional/pathname-aware layout logic first — none found anywhere in this codebase; `(app)` and `(auth)` are the only two route groups, each with its own fixed (non-conditional) layout. Picked the option least invasive to the three routes that must stay unchanged: `/dashboard` moved out of `(app)` into a new sibling group, `(dashboard)`, with its own `layout.tsx` that never imports `AppShell` at all — rather than making `AppShell`/`(app)/layout.tsx` pathname-aware via `usePathname()`. This leaves `(app)/layout.tsx` and `app-shell.tsx` byte-for-byte untouched, so `/menu`, `/menu/new`, `/menu/[id]/edit` carry zero risk of regression from this change (proven by the new `(app)/layout.test.tsx`, VC-CR-002) rather than relying on a conditional branch inside shared code being correct. `(dashboard)/layout.tsx`'s own markup (`min-h-screen bg-slate-50` wrapper, `mx-auto max-w-6xl px-4 py-8` `<main>`) intentionally copies `AppShell`'s non-header markup verbatim, minus the `<header>` block, so the only visible change on `/dashboard` is the missing top bar — not a wider layout/sizing change, which B1 rules out as unrequested scope. The URL `/dashboard` is unchanged (Next.js route groups don't affect URLs), so `login/page.tsx`'s `router.push('/dashboard')` and its tests needed no changes. Reversible structural call, logged per B8. | PROCEED |
| 2026-08-13 | cr-add-menu-category-options (planning) | PROCEED | User found, by hand, in the running app, that `/menu/new`'s Category `<select>` never shows real options — confirmed in code: `add-menu-item-form.tsx`'s option-rendering is hardcoded to `mode === 'edit' && categories`, so `add` mode always falls through to the placeholder-only branch, by an explicit T09-era decision ("no fetch there, per the still-standing T09 decision") that T10 (list screen) and T11 (edit screen) each closed for their own screens but never revisited for the add flow. Unlike the two prior CRs this session, this carries **no discovered risk or scope ambiguity** — it's the exact same `GET /menu/categories` fetch pattern already proven three times (`menu-list-screen.tsx`, `dashboard-screen.tsx`, `edit-menu-item-screen.tsx`), reusing already-shipped, already-auth-header-capable infrastructure (T10's `TokenProvider`). `Build gate: no` — auto-chaining into `/build` directly, no confirmation pause needed. | PROCEED |
| 2026-08-13 | cr-add-menu-category-options T01 (build) | PROCEED | Took option (a) from the task's own dispatch: a new thin wrapper, `AddMenuItemScreen` (`frontend/web/src/modules/menu/components/add-menu-item-screen.tsx`), fetching `GET /menu/categories` only (not `GET /menu/items` — `/menu/new` has no existing item to find/pre-fill, unlike `EditMenuItemScreen`) and rendering `<AddMenuItemForm mode="add" categories={...} />`, with the same loading (`role="status"` spinner)/error (`role="alert"` banner, "Something went wrong loading categories. Try again.") states and `cancelled`-on-unmount guard `EditMenuItemScreen` already established. `app/(app)/menu/new/page.tsx` now renders `AddMenuItemScreen` instead of `AddMenuItemForm` directly. Chosen over option (b) (an internal fetch inside `add-menu-item-form.tsx` itself) to match the existing edit-screen precedent exactly and keep fetch/loading/error concerns out of the shared form component, consistent with `EditMenuItemScreen`'s own already-logged reasoning ("cafe-menu-management T11 (build)"). Also fixed `add-menu-item-form.tsx`'s option-rendering gate: removed the `mode === 'edit' &&` branch entirely — the `<Select>` now always renders the disabled placeholder first, then `categories?.map(...)` appended after it, for either mode. This is a strict generalization of the old edit-mode-only rendering (edit mode gains a placeholder it didn't render before, harmless since a pre-filled `categoryId` still selects its matching real option regardless), and is exactly why `/menu/new` never showed real options even once a `categories` fetch existed for it — the old gate checked `mode`, not whether real data was actually available. Verified the gate change doesn't silently break any existing test expecting add-mode-placeholder-only behavior: every pre-existing `add-menu-item-form.test.tsx` test that renders `<AddMenuItemForm />` (no `mode`/`categories` props) still gets the placeholder-only fallback, since `categories` is `undefined` and `categories?.map(...)` renders nothing — confirmed passing, not just asserted. New tests per B2's accept+reject symmetry: `add-menu-item-screen.test.tsx` (loading/error/success-with-N-options, mirroring `edit-menu-item-screen.test.tsx`'s shape) plus a new `cr-add-menu-category-options T01` describe block in `add-menu-item-form.test.tsx` proving both halves directly on the form — reject (no `categories` prop, and an empty `categories` array, both still placeholder-only) and accept (VC-CR-001's exact N-real-options-plus-placeholder count, plus an AC2 end-to-end POST-with-selected-categoryId test). `pnpm --filter @app/web build/typecheck/lint/test` all green (13 test files, 100 tests, up from 92). No live browser verification performed by this builder call — this session's harness enables only file/Bash tools for the builder role, no browser tool; per the existing T01 (dashboard-CR) precedent, live verification of the rendered `/menu/new` Category dropdown is left for the orchestrating session. | PROCEED |
| 2026-08-13 | cr-in-memory-session (planning) | PROCEED | User explicitly requested removing `localStorage` session persistence ("dont store in local storage, always go with login screen") — this directly reverses login PRD's AC10 ("Given an active session, when I reload the page or open a new tab, then I remain signed in until the session expires or I sign out"), which `session-storage.ts`'s own header comment explicitly chose `localStorage` over an in-memory store *because* it satisfies AC10 ("an in-memory store would fail both [reload and new-tab persistence]" — that's now the explicitly desired outcome, not a rejected tradeoff). Clarified via AskUserQuestion rather than guessed: token moves to in-memory (Redux) state, not zero persistence — normal in-app navigation (dashboard → menu → menu/new, all client-side routing) still works without re-login on every click, but a hard reload, a new tab, or closing the browser loses the session and forces `/login` again. AC6 (redirect away from `/login` if already authenticated) is NOT reversed — it stays meaningful for same-session client-side navigation back to `/login`, just no longer for the reload/new-tab case. **Amends**: `platform/docs/prd/_ACTIVE/login.md`'s AC10 — cited directly even though `login.md` is still `_ACTIVE` (not `_SHIPPED`), same precedent as the dashboard CRs' "not yet shipped via /wrap" exception; login's only remaining gate to `_SHIPPED/` is the unrelated session-expiry-banner HOLD, not this AC. `Build gate: no` — the ambiguity that would normally justify a gate (in-memory vs. zero persistence) was already resolved via an explicit clarifying question to the user before this CR was written, not left for the gate to catch; requiring a second confirmation pass on the same already-confirmed scope would be redundant friction. This CR is the prerequisite for a separate, still-to-be-planned logout+back-arrow CR (needs an in-memory session to actually clear on logout). | PROCEED |
| 2026-08-13 | cr-in-memory-session T01 (build) | PROCEED | Added `store/session.slice.ts` (`{ token: string \| null }`, one `setSessionToken` reducer — no `clearSessionToken`, since logout is explicitly a separate not-yet-planned CR and B1 rules out building for it now) alongside `ui.slice.ts`, registered in `store/index.ts`. `session-storage.ts` was **repurposed, not removed**: `storeSessionToken`/`readSessionToken` keep their exact names and signatures (every call site — `login/page.tsx`, `services.ts`'s `cafeService()` — already imports them by name), only the body changed, from `window.localStorage.setItem/getItem` to dispatching/reading the `session` slice. `services.ts` itself needed **zero changes** — `cafeService()` already passed `readSessionToken` through unchanged as a plain-function `TokenProvider`, and that function's signature/behavior (a plain function, re-read fresh on every call) is identical under the hood. The one real design decision: how a plain function outside the component tree (`readSessionToken`, in turn `cafeService()`) gets at "the" store, given `store/provider.tsx`'s existing `StoreProvider` deliberately creates a **new** store per instance (documented reasoning: "a module-level store would be shared across requests on the server and leak one user's state into another's render"). Considered and rejected: (a) a true module-level singleton (`export const store = makeStore()`) replacing `StoreProvider`'s per-instance creation — would reintroduce exactly the cross-SSR-request leak the existing comment warns about, since `StoreProvider`'s render body (where store creation happens) also executes during SSR, and Node process module state persists across concurrent requests; (b) registering the store reference from inside a `useEffect` in `StoreProvider` — rejected after tracing React's actual effect-firing order: effects fire child-before-parent on mount, so a deeply-nested page's own mount effect calling `cafeService()` could fire *before* `StoreProvider`'s own effect had registered the reference, a real race. Landed on (c): `store/index.ts` exports `setCurrentStore`/`getStore` around a module-level `let`, and `StoreProvider` calls `setCurrentStore(storeRef.current)` synchronously *during its own render body* (same `if (storeRef.current === null)` block that already creates the store), not in an effect. This is safe because React renders parent-before-child (the reverse of effect order): by the time any descendant renders or its effects/handlers run, the reference is already set. It also does not reintroduce the SSR-leak risk in practice for this app specifically: nothing today reads Redux state to produce SSR HTML (`ui.sidebarOpen` always starts `false`; session status is deliberately effect-gated in `login/page.tsx` to avoid a hydration mismatch), so a transient wrong reference during a rare cross-request server-side render interleaving has zero observable effect — nothing consumes it during SSR, and every real consumer (`readSessionToken`/`storeSessionToken`) only ever runs client-side in the browser, in response to a user action or a mount effect. Flagged for whoever adds a future slice that *does* need real per-request SSR-fetched data through this store: this reference mechanism would need re-examining then, since that consumer's assumption (no SSR reads) would no longer hold. `login/page.tsx`'s own logic needed **no changes** — it already imports and calls `storeSessionToken`/`readSessionToken` by name; only its comments (three spots) were corrected, since several explicitly described the now-reversed AC10 reload/new-tab persistence behavior as ongoing fact. Test-file rework: `session-storage.test.ts` fully rewritten (Redux-backed round-trip, AC1 same-store-instance persistence, AC2/VC-CR-002 fresh-store non-persistence, VC-CR-001 zero `Storage.prototype` calls, reject-branch coverage for `storeSessionToken`'s uninitialized-store guard, checkSession validity/expiry unchanged through the new plumbing). `login/page.test.tsx`: file-wide `beforeEach` now seeds a fresh store instead of clearing `localStorage` (`LoginPage` itself never wraps in `<StoreProvider>` — it goes through `session-storage.ts`'s module-level store reference directly, so tests seed that reference rather than rendering an unused Provider); `window.localStorage.getItem/setItem('app.session.token')` assertions replaced with `readSessionToken()`/direct store dispatches; the "storage read failure" AC6 test's mechanism changed from spying on `Storage.prototype.getItem` (no longer meaningful — `readSessionToken` doesn't call it anymore) to forcing the file's existing mocked `readSessionToken` wrapper to throw once, proving the same fail-open guarantee through the new plumbing; the entire "T11 — reload/new-tab session persistence, AC10" describe block (2 tests asserting a second independent mount *still* redirects using a persisted token) was **replaced**, not edited, with a new `cr-in-memory-session — AC1/AC2/VC-CR-001/VC-CR-002` describe block, since those two old tests now assert the exact behavior this CR reverses — keeping them passing by weakening their assertions would be B3 test suppression; removing them outright without new coverage would leave AC2/VC-CR-002 unproven. One real bug caught mid-build by a first failing test run (not silently worked around): the new VC-CR-001 test initially reused this file's usual `'a.b.c'` placeholder token, which isn't valid base64url JSON — the real `checkSession` correctly read it as "no session" on the test's second `LoginPage` mount, so the redirect assertion failed for the right reason. Fixed by using a real JWT-shaped token via the file's existing `makeToken` helper, not by loosening the assertion. `pnpm --filter @app/web build/typecheck/lint/test` all green (14 test files, 110 tests, up from 107 pre-CR); `next build`'s route table confirms every route (including `/login`) still compiles with the same static/dynamic split as before. No live browser verification performed by this builder call — this session's harness enables only file/Bash tools for the builder role, no browser tool; per the existing T01 (dashboard-CR / add-menu-category-options) precedent, live verification (in-app navigation keeping the session, a real hard reload losing it, DevTools confirming no `app.session.token` key in either Storage) is left for the orchestrating session. | PROCEED |
| 2026-08-13 | cr-logout-and-back-navigation (planning) | PROCEED | Second half of the user's original two-part request ("add a logout button and back arrow on each screen"), planned now that `cr-in-memory-session` (its prerequisite — logout needs an in-memory session to actually clear) is done. Back-arrow behavior confirmed via clarifying question: browser-back (`router.back()`), not a fixed per-screen destination. Scope: the four authenticated app screens (`/dashboard`, `/menu`, `/menu/new`, `/menu/[id]/edit`) — `/login`/`/forgot-password` excluded, no "back"/"logout" concept applies pre-authentication. **Amends**: `platform/docs/prd/_ACTIVE/login.md`'s §5 "Out of scope" bullet, "Sign-out / logout capability" (still `_ACTIVE`, same not-yet-shipped-via-/wrap caveat as the other login-amending CRs) — this CR is what finally picks that up, as that bullet's own text anticipated ("Logout is now the dashboard PRD's responsibility, to be picked up when that PRD is planned" — no formal "dashboard PRD" exists, the CR mechanism fills that role instead). Noted for the builder: `session.slice.ts` currently has no `clearSessionToken` action (`setSessionToken`'s payload is typed `string`, not `string | null`) — deliberately not built ahead of need per B1 when `cr-in-memory-session` shipped; this CR is what needs it. Logout's natural UI home differs per screen: the dashboard already has an (inert, decorative) "User menu" `IconButton` in its own header; `AppShell` (used by `/menu`/`/menu/new`/`/menu/[id]/edit`) has no equivalent today and needs one added. `Build gate: no` — both open questions (back-arrow mechanism, screen scope) were already resolved via clarifying question before this CR was written. | PROCEED |
| 2026-08-13 | cr-logout-and-back-navigation T01 (build) | PROCEED | Session-clearing plumbing: added a dedicated `clearSessionToken` action to `session.slice.ts` (not a widened `setSessionToken(string \| null)`) — every existing `setSessionToken` call site (`storeSessionToken`) always has a real token in hand, and widening its payload type would give it a null branch it never uses just to serve one new caller. `session-storage.ts` gets a `clearSessionToken()` function following `storeSessionToken`'s exact shape (same `getStore()` call, same fail-closed-if-uninitialized guard, reused not duplicated). | PROCEED |
| 2026-08-13 | cr-logout-and-back-navigation T01 (build) | PROCEED | Dashboard (AC1): the existing inert "User menu" `IconButton` (avatar + name + chevron) in `TopHeader` was wired directly to logout — its `disabled` was removed, `onClick` now clears the session and pushes `/login`, and its accessible name changed from "User menu" to "Log out" so a screen reader announces what it actually does now. No second, redundant logout control was added next to it: a real dropdown-with-a-logout-item would be scope creep (B1) for what this CR asks (a working logout control, not a user menu), so the existing control's behavior was simply made real. Back arrow (AC2) is new — this screen had no "back" concept before — added as an `IconButton` (`ArrowLeft`, `aria-label="Back"`, `onClick={() => router.back()}`) at the start of `TopHeader`, before the existing (still-inert) hamburger button. | PROCEED |
| 2026-08-13 | cr-logout-and-back-navigation T01 (build) | PROCEED | `AppShell` (shared by `/menu`, `/menu/new`, `/menu/[id]/edit` via `(app)/layout.tsx`) gets both controls added once, covering all three routes: a `Button` (`variant="secondary"`, `size="sm"`, `LogOut` icon + "Log out" text, `ml-auto`) that clears the session and pushes `/login`, and an `IconButton` back arrow (`ArrowLeft`, `aria-label="Back"`, `onClick={() => router.back()}`) placed after the mobile sidebar toggle, before the brand link. Both surfaces call `useRouter()` from `next/navigation`, matching `login/page.tsx`'s already-established pattern in this codebase — no new router mechanism introduced. Existing `(app)/layout.test.tsx` and `dashboard-screen.test.tsx` needed a `next/navigation` mock added (previously unused by either) since `AppShell`/`TopHeader` now call `useRouter()`; no other existing assertion in either file needed to change. New tests added directly against `AppShell` (`components/app-shell.test.tsx`, new file) and `DashboardScreen` for AC1 (session cleared + `push('/login')`) and AC2 (`router.back()` called, `router.push` not called) on both chrome surfaces, plus an AC3 test on `DashboardScreen` (adapted from its pre-existing `success: false` error-state test, with a real `clearSessionToken()` call first) proving the screen's already-built error handling still fires post-logout with no new guard added. `pnpm --filter @app/web build/typecheck/lint/test` all green. No live browser verification performed by this builder call — this session's harness enables only file/Bash tools for the builder role, no browser tool; left for the orchestrating session per existing precedent (dashboard-CR, add-menu-category-options, in-memory-session T01s). | PROCEED |
| 2026-08-13 | cr-logout-and-back-navigation T01 (review cycle 1 rework) | PROCEED | BLOCKER: the AC3 test in `dashboard-screen.test.tsx` hardcoded the module-mocked `cafeService().get()` (`getMock`) to always return `success: false`, so the `storeSessionToken`/`clearSessionToken()` calls at the top were structurally disconnected from the mocked outcome — deleting both lines left the test passing unchanged, meaning it never actually exercised "a real logout causes the next fetch to be unauthenticated." Reviewer offered two directions: (a) make `getMock` branch on `readSessionToken()`'s live value so the test is wired to real session state, or (b) drop the test as a duplicate of `session-storage.test.ts`/`session.slice.test.ts`'s already-adequate `clearSessionToken()` coverage plus the orchestrator's own live-verification note. Took (a): `getMock`'s implementation now checks `readSessionToken()` at call time — a present token gets the same success payloads the rest of the file uses, a cleared one gets the `success: false` shape the dashboard's existing error state already handles. This ties the mock to the same `readSessionToken()` function the real `cafeService()` is built on top of (`services.ts:53-58`), so a `clearSessionToken()` that regresses to a no-op leaves the mock still returning success and the test fails on the missing `role="alert"`. Chose (a) over (b) because AC3 is specifically about the causal link between logout and the next fetch's outcome, not just that `clearSessionToken()` clears a value in isolation (already covered elsewhere) — B2 expects a test at the AC-bound integration point, not just at the underlying primitive. Manually verified falsifiability before finalizing: temporarily commented out the `clearSessionToken()` call, re-ran this file alone — the AC3 test failed on the `waitFor(() => screen.getByRole('alert'))` timeout, for exactly the right reason (mock still saw the token, returned success); restored the real call, re-ran, green. `review_cycles` incremented to 1 in the ledger, `status` left at `in_review` for the reviewer. NIT (chrome-surface structural placement drift) left as-is per the orchestrating session's instruction — cosmetic, not required. | PROCEED |
| 2026-08-13 | cr-menu-item-save-feedback (planning) | PROCEED | User-reported "Save Item button is not working" — investigated live before assuming a bug: the `POST /menu/items` call genuinely succeeds (`201 Created`, independently confirmed the item exists via a direct API call after the fact), so this is not a functional failure. The actual gap: on a successful save, the form silently resets to empty with zero feedback — no success message, no redirect, nothing distinguishing it from a silent failure, which is exactly why it read as "not working." **Amends**: `platform/docs/prd/_SHIPPED/cafe-menu-management.md` (T09's `add-menu-item-form.tsx` — the form already has an established error-banner pattern, `SUBMIT_ERROR_MESSAGE`, for the failure path; this CR adds the missing symmetric success path). Scoped to the add flow specifically (what was reported); edit mode's own save-feedback is a related but separate gap, not included here — noted for awareness, not fixed. Chosen fix direction: a success banner/message shown after a successful save, with the form staying on-screen (not auto-redirecting to `/menu`) — matches an admin doing bulk entry (the exact scenario used to seed this session's demo data), a more conservative default than presuming the user wants to leave the screen. `Build gate: no` — low-risk, additive-only (a missing success state, not a behavior change to anything working). | PROCEED |
| 2026-08-13 | cr-menu-list-navigation-link (planning) | PROCEED | User-reported "cafe-menu-list-default screen navigation is not available in menu management" — confirmed: there is no link from `/dashboard` to `/menu` (the list screen, `cafe-menu-list-default.png`) anywhere in the app; the dashboard's "Menu Management" nav item only ever went to `/menu/new` (add form), a choice made in `cr-dashboard-menu-management-link`. **This CR reverses that specific choice**, not just adds something new: repoints "Menu Management" to `/menu` (the list/overview screen — the more conventional meaning of "menu management" as a section, matching the wireframe's own screen name), relying on `/menu`'s own already-existing "+ Add Item" button (built in `cafe-menu-management` T10) as the path to the add form one click further in, rather than the reverse. **Amends**: `platform/docs/prd/_CHANGE_REQUESTS/cr-dashboard-menu-management-link.md`'s AC4 (still `_CHANGE_REQUESTS/`, not `_SHIPPED/` — same not-yet-shipped-via-`/wrap` caveat as other CRs amending CRs) and, transitively, `cafe-menu-management`'s own AC3/AC5/AC6 (T10, `_SHIPPED/`) which the list screen already implements — this CR only changes what links to it, not the screen itself. `Build gate: no` — the destination already exists, built, and tested; this is a link-target change, not new functionality, low risk. | PROCEED |
| 2026-08-13 | cr-menu-item-save-feedback T01 (build) | PROCEED | No dedicated `Alert`/`Banner` component exists in `@app/ui` (checked first, per this task's own dispatch — only `Badge`, `Button`, `Card`, `Field`, `Select`, `IconButton`) — the existing `SUBMIT_ERROR_MESSAGE`/validation-banner pattern in `add-menu-item-form.tsx` is itself local, inline-styled markup (`<p role="alert" className="...">`), not a shared component. Reused that exact established local pattern for the success state rather than inventing new markup or a new shared component: same layout (icon + text, `rounded-md border px-3 py-2 text-sm`), green (`green-200`/`green-50`/`green-700`) in place of red, `CheckCircle2` (already used for a success state on `dashboard-screen.tsx`) in place of `AlertCircle`. Used `role="status"` rather than `role="alert"` for the success banner — `role="alert"` is an assertive live region reserved in this file for validation/submit failures that need to interrupt; a save confirmation is informational, matching this file's own `role="status"` precedent (`AddMenuItemScreen`'s loading spinner) for non-error states. Copy: `"<name> added"` (e.g. "Spring Rolls added"), the exact form of AC1's own example, using the just-submitted trimmed name rather than the POST response's echoed `data.name` — the two are expected to match, but the locally-known value avoids a dependency on the response shape for display copy. Scoped to `'add'` mode only (`POST /menu/items`) per the CR's own text ("After a successful `POST /menu/items`") — `'edit'` mode's `PATCH` success-feedback is a related but separate, not-yet-requested gap (also flagged in this CR's own planning-decision row above), left untouched. AC2 "clears/doesn't persist across an unrelated subsequent action": did **not** add any auto-clearing of the form fields themselves (the CR's Why-section language about the form "silently resetting to empty" describes the *current*, no-feedback bug's felt effect, not literal reset code — no such reset exists in `handleSubmit` today, confirmed by reading it), since field-clearing isn't one of AC1-AC3's requirements and adding it would be scope creep per B1. Instead, the success *message* — the thing AC2 actually pins — is cleared in two places: at the start of every new submit attempt (so a second save, success or failure, never shows a stale first-save confirmation while in flight), and on every field `onChange` (name/category/price), so starting an unrelated edit after a successful save can't be mistaken for confirming that new, not-yet-saved change. AC3 (failure path unchanged): no line inside the existing `submitError`/`showValidationBanner` state or their rendering was touched; the new `successMessage` state and its one `<p role="status">` block were added purely additively. Tests: `add-menu-item-form.test.tsx`'s new `cr-menu-item-save-feedback T01` describe block — AC1 (success message names the saved item, form stays rendered/no unmount), AC2 (no message on initial render; message clears on a subsequent field edit; message clears once a second submit is in flight), AC3 (a failed save shows only the existing error banner and never the success message; an invalid submit shows only the existing validation banner and never the success message). Falsifiability independently verified: temporarily removed the `setSuccessMessage(buildSuccessMessage(trimmedName))` call and re-ran this file alone — exactly the 3 success-dependent tests failed, the AC2-page-load and both AC3-unchanged tests stayed green as expected; restored the real call and re-ran the full four-gate suite (`pnpm --filter @app/web build/typecheck/lint/test`) clean (16 test files, 130 tests, up from 124). No live browser verification performed by this builder call — no browser tool available to this role; left for the orchestrating session, per existing precedent noted in prior CR build entries (e.g. `cr-logout-and-back-navigation T01 (build)`). Pre-existing dirty `git status` at task start (scaffold/docs/`.claude` process files, unrelated to and untouched by this task) reported but not worked around — nothing under `platform/frontend` or `platform/backend` was dirty, so it did not conflict with this task's file set. | PROCEED |
| 2026-08-13 | cr-menu-item-save-feedback T01 (review cycle 1 SHOULD-FIX) | PROCEED | SHOULD-FIX: the 'add'-mode `POST /menu/items` branch set `successMessage` unconditionally once `cafeService().post(...)` resolved, without checking `response.success` first — unlike the sibling 'edit'-mode PATCH branch a few lines above, which already does `if (!response.success) throw new Error(...)`. Made the POST branch symmetric with the PATCH branch: now checks `response.success` and only treats the save as successful (sets `successMessage`) when true, otherwise throws into the same `catch`/`SUBMIT_ERROR_MESSAGE` path the PATCH branch already uses. Currently unreachable via the real backend (its controller only ever resolves a 2xx with `success: true`), so no existing test's assertions changed; added one new test in `add-menu-item-form.test.tsx` mocking `cafeService().post()` resolving `{ success: false, ... }` on a 2xx-shaped response, asserting the success banner does not appear and the existing error-banner path fires instead (131 tests, up from 130). `pnpm --filter @app/web build/typecheck/lint/test` all green. `review_cycles` incremented to 1, `status` set to `done`. | PROCEED |
| 2026-08-13 | cr-menu-list-navigation-link T01 (build) | PROCEED | One-line change, exactly as scoped: `dashboard-screen.tsx`'s `NAV_SECTIONS` entry for "Menu Management" now has `href: '/menu'` (was `/menu/new`) — the only occurrence of that link target in the file (`AddMenuItemLink`'s own separate `/menu/new` href, AC2, untouched). Also updated the file's own top-of-file header comment, which previously stated as fact that "Menu Management" and "+ Add Menu Item" "both route to /menu/new" — left unedited it would have been actively wrong about the code beneath it, so it now reflects the new split (Menu Management -> `/menu`, Add Menu Item -> `/menu/new` unchanged) and cross-references this CR. Confirmed via `Glob` that `/menu` (`(app)/menu/page.tsx`, the `cafe-menu-management` T10 list screen) exists and is fully built before making the change, per this task's own instruction not to assume the route. Confirmed via `Grep` that `/menu`'s own "+ Add Item" link (`menu-list-screen.tsx`) still points at `/menu/new`, untouched (AC2) — that screen was not opened for editing, only grepped to verify. Test changes to `dashboard-screen.test.tsx`: the old combined "AC4/AC5 — the Menu Management nav link and the Add Menu Item button both resolve to /menu/new" test (left over from `cr-dashboard-menu-management-link`) was split into two, rather than just changing its one assertion — a single test now asserting two unrelated things under two different ACs (one now-changed, one not) would have made a future regression on either half harder to attribute to the right AC. New `AC1` test asserts the Menu Management link's href is `/menu`; new `AC3` test asserts the Add Menu Item link's href is still `/menu/new` and that exactly two real `<a>` links exist on the screen (nothing else was turned into or out of a link). AC2 (the list screen's own "+ Add Item" still resolving to `/menu/new`) already has standing, untouched coverage in `menu-list-screen.test.tsx` (`"title/subtitle/"+ Add Item" render, and Add Item links to /menu/new"`) — confirmed present rather than adding a redundant duplicate, per this task's own instruction. `VC-CR-002`'s existing inert-nav-item test and the AC3-logout test in the same file needed no changes — neither reads the Menu Management href. `pnpm --filter @app/web build/typecheck/lint/test` all green (16 test files, 132 tests, up from 131 — net +1 from the one-test-split-into-two). No live browser verification performed by this builder call — no browser tool available to this role; left for the orchestrating session, per existing precedent in prior CR build entries. | PROCEED |
| 2026-08-13 | cr-menu-edit-item-save-feedback (planning) | PROCEED | User-reported "Edit menu is also giving error" after `cr-menu-item-save-feedback` shipped for the Add screen. Investigated live before assuming a new bug: reproduced cleanly in a fresh, un-contaminated browser tab (logged in, real in-app `Link` navigation only, no hard-nav session loss) — the `PATCH /menu/items/:id` call genuinely succeeds (200 OK, price change confirmed persisted on reload), zero console errors, zero error banner rendered. This is not a new functional defect — it is the exact same missing-success-feedback gap `cr-menu-item-save-feedback` diagnosed for Add, now surfacing for Edit, which that CR's own planning row explicitly called out and excluded ("edit mode's own save-feedback is a related but separate gap, not included here — noted for awareness, not fixed"). Confirmed in code: `add-menu-item-form.tsx`'s `'edit'`-mode PATCH branch (`handleSubmit`) only guards `if (!response.success) throw ...` — on success it falls straight through with no `setSuccessMessage` call and no banner of any kind, unlike the sibling `'add'`-mode POST branch this CR's predecessor already fixed. **Amends**: `platform/docs/prd/_CHANGE_REQUESTS/cr-menu-item-save-feedback.md` (still `_CHANGE_REQUESTS/`, not yet shipped via `/wrap`) — extends its excluded edit-mode scope rather than reopening its own (already-reviewed, done) T01. Chosen fix: reuse the exact same `successMessage` state/banner/clearing mechanism already built for Add, firing from the PATCH branch on the `!response.success` guard passing, with copy `"<name> updated"` (mirroring `"<name> added"`'s exact shape) to stay distinct from the add-flow's own wording. Edit form already stays on-screen after a PATCH (no redirect exists today, unaffected) — matches the same "admin editing several items" precedent as Add's own stay-on-screen decision. `Build gate: no` — same low-risk, additive-only shape as the CR it extends; reuses an already-reviewed pattern, no new mechanism. | PROCEED |
| 2026-08-14 | cr-session-guard-redirect-to-login (planning) | PROCEED | User-reported: refreshing the browser on `/dashboard` shows "Something went wrong loading the dashboard. Try again." — expected behavior per the user is that a refresh (which loses the in-memory session by `cr-in-memory-session`'s own deliberate design) should redirect straight to `/login`, not show a generic fetch-error banner. Investigated: this is not unique to the dashboard. All four protected screens (`dashboard-screen.tsx`, `menu-list-screen.tsx`, `add-menu-item-screen.tsx`, `edit-menu-item-screen.tsx`) share the identical pattern — a mount `useEffect` fires `cafeService()` calls unconditionally, with no check for whether a session even exists first. On a refresh, the token is gone (by design), the backend correctly 401s, and each screen's own generic catch block folds that into its own `LOAD_ERROR_MESSAGE` ("Something went wrong loading ___. Try again.") — technically correct (the fetch did fail) but the wrong *class* of failure to show the user: this is "you're signed out," not "the server had a problem, try again." `cr-in-memory-session`'s own T01 build note explicitly flagged this exact gap in passing without fixing it (`session.ts`'s own header comment: "There is deliberately no app/dashboard mount-time... integration... this module has no caller there today" — written when `/dashboard` was still a placeholder, now stale). **Amends**: all four screens above, plus `frontend/libs/core/src/session.ts`'s header comment (the "no dashboard-side use of this function today" line becomes false once this CR lands). Chosen fix: add one new shared client hook, `frontend/web/src/lib/use-require-session.ts` (not `@app/frontend-core` — routing/DOM concerns stay web-app-local, same reasoning `session-storage.ts` already documents for itself), wrapping the already-built, already-tested `checkSession(readSessionToken())` primitive from `@app/frontend-core`. On mount, if no valid session, it calls `router.push('/login')` and reports "not ready"; each of the four screens' own data-fetch effect is gated on this hook's "ready" signal, so an unauthenticated mount now redirects immediately and never fires the doomed fetch or shows the generic error banner at all. A *valid* session's behavior is completely unchanged (same fetches, same loading/ready/error states for genuine mid-session failures, e.g. a real network error or 500). One hook, reused four times, rather than four separate copies of the same check — mechanical, not a new pattern. `Build gate: no` — reuses an existing, already-reviewed, already-tested primitive (`checkSession`); the only new code is the thin hook wrapper and four small, mechanical call-site changes. | PROCEED |
| 2026-08-13 | cr-menu-edit-item-save-feedback T01 (build) | PROCEED | Reused the exact `successMessage` state/banner already built by `cr-menu-item-save-feedback T01` rather than adding a second state/banner pair — only one `mode` is ever mounted per screen instance, so one banner serving both `'add'` and `'edit'` copy is not a conflict, per this task's own dispatch. Added a second small helper, `buildUpdateSuccessMessage(itemName)` (`"${itemName} updated"`), mirroring `buildSuccessMessage`'s exact shape with "updated" in place of "added" — kept as a second named function rather than parameterizing the existing one with a verb argument, since the two helpers read as self-documenting call sites in the two branches (`setSuccessMessage(buildSuccessMessage(...))` / `setSuccessMessage(buildUpdateSuccessMessage(...))`) without a caller needing to know or pass a magic string. Set inside the `'edit'`-mode PATCH branch immediately after the existing `if (!response.success) throw ...` guard passes (already present, untouched) — so the success path was purely additive, no new conditional logic. Used the just-submitted trimmed `name`, same reasoning as the sibling POST branch (`cr-menu-item-save-feedback T01`'s own decision row): the locally-known value avoids a dependency on the PATCH response's echoed shape for display copy, and the two are expected to match. AC2's clearing (on every field `onChange` and at the start of every new submit attempt) needed **no new code** — those clears were already written mode-agnostically in `cr-menu-item-save-feedback T01` (the `onChange` handlers and the top of `handleSubmit` don't branch on `mode`), so 'edit' mode inherited them for free; confirmed by reading before assuming, rather than adding a redundant duplicate clear. AC3 (failure path and the availability toggle unchanged): the toggle's own `isAvailable`/`toggleError`/`handleToggleAvailability`/`TOGGLE_ERROR_MESSAGE` code was not touched at all — confirmed by diff review before running gates — and the existing `if (!response.success) throw ...` guard on the Save-Item PATCH branch (added in the T11 build, predating this CR) already routes a `success: false` response into the same `catch`/`SUBMIT_ERROR_MESSAGE` path a thrown/network error uses, so no new guard was needed there either, only a test proving it still holds. Tests added to `add-menu-item-form.test.tsx`'s existing `'edit mode (T11)'` describe block (reusing its `fixtureItem`/`fixtureCategories`/`patchMock`, not re-declared) under a new nested `cr-menu-edit-item-save-feedback T01` describe: AC1 (success message names the saved item with "updated" copy, distinct from "added", form stays rendered), AC2 (no message on initial render of the edit form; message clears on a subsequent field edit; message clears once a second submit is in flight), AC3 (a failed PATCH shows only the existing `SUBMIT_ERROR_MESSAGE` banner and never the success message; a resolved PATCH with `success: false` falls into the same error path rather than showing success; the availability toggle's own success/failure — reusing the file's existing toggle assertions — still flips the badge via its own PATCH without ever touching the Save-Item success banner). `pnpm --filter @app/web build/typecheck/lint/test` all green (16 test files, 139 tests, up from 132 — 7 new tests). No live browser verification performed by this builder call — no browser tool available to this role; left for the orchestrating session, per existing precedent in prior CR build entries (e.g. `cr-menu-item-save-feedback T01 (build)`). Pre-existing dirty `git status` at task start (scaffold/docs/`.claude` process files, unrelated to and untouched by this task) reported but not worked around — nothing under `platform/frontend` or `platform/backend` was dirty, so it did not conflict with this task's file set. | PROCEED |
| 2026-08-14 | cr-session-guard-redirect-to-login T01 (build) | PROCEED | Several judgment calls, none security/data-integrity, all reversible: (1) `useRequireSession`'s effect deliberately mirrors /login page.tsx's own T11 mount-check exactly — an empty `[]` dependency array, not `[router]` — per the task's own instruction to match that existing precedent rather than pick independently; the reasoning carries over unchanged (`useRouter()` is stable in practice; re-running on router-identity change would risk more than one `router.push('/login')` for the same missing session). (2) Fail-open direction on a thrown `checkSession`/`readSessionToken` call: redirects to /login (treats a broken check as "no session"), the mirror image of /login's own T11 guard (which fails open to the sign-in form). Chosen because the two guards protect opposite things — /login's guard decides whether to *skip* the sign-in form (worst case: ask an already-signed-in user to sign in again, harmless); this guard decides whether to let a protected screen's *data fetch* fire (failing open the other way would let the fetch run against a session state that couldn't even be confirmed, risking the exact 401-into-generic-banner bug this CR exists to fix). (3) Existing tests that seeded a session with the placeholder `storeSessionToken('a.b.c')` (dashboard-screen.test.tsx, both under cr-logout-and-back-navigation T01) had to move to a real JWT-shaped token (`validToken()`, same hand-rolled shape login/page.test.tsx's own T11 suite uses) — `'a.b.c'` fails `checkSession`'s real decode step, so with the new guard wired in, `useRequireSession` would reject it as invalid and redirect before the fetch could ever fire, breaking those tests for a reason unrelated to what they're about. (4) One existing test's *assertion*, not its setup, had to change: dashboard-screen.test.tsx's "AC3 — after a real logout clears the session, the existing error state still fires on a failed fetch" asserted the exact pre-CR bug (a cleared session still lets the fetch run and 401 into the generic banner) as correct behavior. That assertion is now false on purpose — this CR's whole point is that mounting with no session redirects instead. Rewritten (same "real logout, then remount" setup) to assert the new, correct behavior (redirect, no fetch call, no banner) rather than deleted, weakened, or left contradicting the shipped code (B3). (5) `session.ts`'s `checkSession` function-level JSDoc carried the same now-false "no dashboard-side use of this function today" claim as the module header the task named directly — updated both for consistency, not just the one the task quoted, since leaving a second stale copy of the same false claim in the same file would be its own small doc-rot. Falsifiability check (B2): temporarily disabled the guard (`hasSession` forced `true`, effect body no-op) and reran the 6 new/updated AC1 tests across the hook and all four screens — all 6 failed for the expected reason; restored the real implementation and reran the full `@app/web` suite (146/146 green). Pre-existing dirty `git status` at task start (scaffold/`.claude` process files from prior sessions, unrelated to and untouched by this task) reported but not worked around — nothing under `platform/` was dirty. | PROCEED |
| 2026-08-14 | cr-session-guard-redirect-to-login T01 (review cycle 1 SHOULD-FIX) | PROCEED | SHOULD-FIX: this CR's own `Amends` field named the four screens' fetch effects and `session.ts`'s comment, but not `cr-logout-and-back-navigation.md`'s AC3 — whose text ("no new guard required by this CR itself... the screen's own existing loading/error handling... takes over from there") this task's shipped code now factually contradicts (a guard *was* added, and post-logout the old error-handling path is deliberately bypassed in favor of a redirect). Closed by: (1) adding a superseded-note directly under `cr-logout-and-back-navigation.md`'s AC3 explaining what changed and why, dated and cross-referenced to this row; (2) adding that file to this CR's own `Amends` field, matching the `cr-menu-list-navigation-link` precedent for CR-amends-CR citations. No code change — this was a documentation-consistency gap, not a behavior defect (the reviewer's own review confirmed the underlying behavior itself, and the rewritten dashboard-screen.test.tsx AC3 test proving it, are both correct and not suppressive). `review_cycles` incremented to 1, `status` set to `done`. | PROCEED |
| 2026-08-14 | cr-dashboard-live-identity-date-and-nav-affordance (planning) | PROCEED | Three user-requested polish items on the dashboard, bundled into one CR since all three touch the same file (`dashboard-screen.tsx`) and were requested together in one message: (1) "Good morning, John!" and the header's "John Smith"/"Admin"/"JS" are hardcoded wireframe placeholder text, never tied to the actual logged-in user; (2) "12 Aug 2025" next to the welcome message is likewise a hardcoded, now-stale mock date; (3) every left-nav item other than "Menu Management" is a `disabled` `<button>` (deliberate, per `cr-dashboard-menu-management-link`'s own scope), which the user wants changed to *enabled*-but-still-non-navigating, i.e. visually/functionally clickable but a no-op. **Checked the actual data available before designing a fix**: `backend/auth`'s `User` entity (`user.entity.ts`) has no name column at all — `id`, `email`, `passwordHash`, `role`, timestamps only; `loginUserSchema`/`LoginUser` (`shared/contracts`) is `{id, email}`; the signed JWT payload is `{sub, email, role}` (`auth.service.ts:56`). There is no "name" anywhere in this system to display — inventing one (a new column/migration/contract field) would be a materially bigger, cross-boundary change than what was asked, and CLAUDE.md #6 requires a contract-first change for that, not a CR-sized fix. Chosen fix for (1): derive a display name from the email's local-part (e.g. `admin@example.com` -> "Admin"), the only real, already-available identity data — reusing the existing, already-tested `checkSession(readSessionToken())` primitive (same one `useRequireSession` already wraps for the redirect guard) via a new, separate, read-only hook `useSessionUser()` (`frontend/web/src/lib/use-session-user.ts`) rather than widening `useRequireSession`'s already-reviewed, already-shipped boolean contract across its four existing call sites for a display-only need only the dashboard has. Role text ("Admin") is left hardcoded, not decoded from the JWT's `role` claim — out of this CR's stated scope (only name + date were asked), and the current demo account's role genuinely is Admin, so nothing is currently false; flagged here for whoever eventually wires role-based UI, not silently fixed. Fix for (2): compute the real current date client-side (`new Date()`), formatted to match the mock's existing style (`day month year`, e.g. "14 Aug 2026") — purely presentational, no timezone/locale AC exists to pin a specific one, reversible per B8. Fix for (3): remove `disabled` from `InertNavItem`, add the same `hover:bg-slate-100` affordance `NavLink` (the one real nav link) already uses so an inert item now *looks* exactly as clickable as a real one, and add an explicit no-op `onClick` (documented, not just an absent handler) so the "does nothing on click" behavior is asserted by intent, not by omission — matching this file's own established comment style for why a control is inert. VC-CR-002's existing test (`dashboard-screen.test.tsx`) asserted `disabled: true` on every inert item; that assertion is now the wrong thing to prove and must be updated to `disabled: false` while keeping (and this is the part that must not regress) the "no navigation, same URL, no console error" half exactly as strict as before — a rework of the assertion to match the new, explicitly-requested requirement, not a weakening of what's being verified (B3). `Build gate: no` — three small, independent, low-risk display/affordance changes, no new backend/contract surface, reuses an already-tested session primitive. | PROCEED |
| 2026-08-14 | cr-dashboard-live-identity-date-and-nav-affordance T01 (build) | PROCEED | Implemented per the CR/planning row above, no deviations. Judgment calls, all reversible: (1) `useSessionUser`'s "not resolved yet" state is `null`, and `DashboardScreen` maps that to `''` for both `displayName`/`initials` — WelcomeSection renders `Good morning! 👋` (no comma/name) and TopHeader renders empty name/initials spans for that brief pre-resolution window, rather than a loading spinner or a second "not ready" UI state; this is purely cosmetic display (not a gate, per the task's own framing) and the window is a single mount-effect tick, the same shape `useRequireSession` itself already has before it flips to `true`. (2) `formatWelcomeDate` uses a hand-rolled 12-entry short-month-name array rather than `Intl.DateTimeFormat`/`toLocaleDateString` — this file (and the rest of the module) has no existing date-formatting precedent either way, and a fixed table keeps the rendered string deterministic regardless of the test/CI runtime's ICU data, with no new dependency (B6). (3) `displayNameFromEmail`/`avatarInitialsFromEmail` live in the new `use-session-user.ts` file alongside the hook itself (not inlined in `dashboard-screen.tsx`) — the task left this as a free choice; co-locating them with the hook that supplies the raw email keeps `dashboard-screen.tsx`'s own diff smaller and makes both pure helpers independently unit-testable without rendering the full screen. (4) `InertNavItem`'s `onClick={(event) => event.preventDefault()}` takes the `preventDefault` form the task suggested (over a bare empty arrow function) since it's marginally more self-documenting at the call site (reads as "this button intentionally swallows its own click") even though a `<button type="button">` with no ambient `<form>` has no default action to prevent either way — purely a style pick, not a behavior difference. Tests: `use-session-user.test.ts` (new, 7 tests: `useSessionUser`'s no-session/expired/valid branches mirroring `use-require-session.test.ts`'s own shape, plus `displayNameFromEmail`/`avatarInitialsFromEmail` accept-case unit tests); `dashboard-screen.test.tsx` (3 new tests: AC1 header derivation from the suite's default `ada@example.com` session, AC1 a second, distinctly different email — `jordan.lee@example.com` — to prove genuine per-email dependence rather than a coincidental stand-in string, AC2 real-date assertion computed the same way the component does rather than a frozen past date; plus the pre-existing "renders the dashboard title..." test's hardcoded `Good morning, John! 👋` assertion updated in place to the now-correct `Good morning, Ada! 👋`, and VC-CR-002's `disabled: true` assertion changed to `disabled: false` with its "no navigation, no console error" assertions left exactly as strict as before). Falsifiability check per B2: temporarily restored `InertNavItem`'s `disabled` attribute and reran VC-CR-002 — failed for the expected reason (`disabled` true vs expected false); reverted, reran the full suite, 156/156 green. `pnpm --filter @app/web build/typecheck/lint/test` all green (18 test files, 156 tests, up from 146). No live browser verification performed by this builder call — no browser tool available to this role, per existing precedent in prior CR build entries (e.g. `cr-menu-edit-item-save-feedback T01`); left for the orchestrating session. Pre-existing dirty `git status` at task start (scaffold/docs/`.claude` process files from prior sessions, unrelated to and untouched by this task) reported but not worked around — nothing under `platform/` was dirty, so it did not conflict with this task's file set. | PROCEED |
| 2026-08-14 | cr-dashboard-live-identity-date-and-nav-affordance T01 (review cycle 1 SHOULD-FIX) | PROCEED | SHOULD-FIX: VC-CR-002's "every left-nav item other than Menu Management is inert" test only asserted `window.location.href` stayed unchanged after each inert item's click — it never asserted the mocked `pushMock` was not called, so a hypothetical future regression wiring `router.push(...)` onto one of these items' onClick wouldn't move `window.location.href` in jsdom and this test wouldn't catch it, even though the file's own back-arrow test elsewhere already checks `pushMock` for exactly this kind of thing. Closed by adding `expect(pushMock).not.toHaveBeenCalled()` once, after the click loop finishes (`dashboard-screen.test.tsx`) — no per-iteration duplication needed since `pushMock` is reset in `beforeEach` and never expected to be called at any point in this test. No other code changed. `pnpm --filter @app/web build/typecheck/lint/test` all green (18 test files, 156 tests, unchanged count — same test, one added assertion). NIT (AC2 date test independently reimplementing a short-month-names array rather than importing the component's own) left as-is, per the reviewer's own explicit call that it was acceptable. `review_cycles` incremented to 1, `status` set to `done`. | PROCEED |
| 2026-08-14 | cr-dashboard-recent-menu-changes T01 (build) | PROCEED | Implemented per the CR, no deviations. Judgment calls, all reversible (B8): (1) Timestamp format — reused `formatWelcomeDate`'s day/short-month/year output and appended zero-padded `hours:minutes` (new `formatRecentChangeTimestamp` helper, same file) rather than inventing a second independent format or a relative "2 hours ago" style; a bare date alone wouldn't distinguish same-day changes within the 5-item list, and no new dependency was added (B6), consistent with `formatWelcomeDate`'s own precedent. (2) Loading/error state for the "Recent Menu Changes" card — gated purely on `isReady` (renders nothing while loading or on fetch error), deferring to the Menu Overview table's existing shared spinner/error banner above it rather than building a third, separate state machine for one card; the task's own instructions explicitly allowed either this or a lightweight independent indicator, and this is the simpler of the two with no loss of information (the KPI cards above already do the same "value is null/absent until ready" pattern). (3) Visual treatment: "Added" entries use `Badge kind="ok"` (green), "Updated" use `kind="info"` (sky) — no wireframe pins colors for this card (`wireframe: none` in the ledger), so this borrows the same green-for-positive/blue-for-neutral-informational convention the Menu Overview table's own Active/Inactive badges already establish in this file. (4) Sort implemented as `[...items].sort((a, b) => new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime())` — a fresh array via spread, never `items.sort(...)` in place, since `items` is still read by `categoryRows`/the KPI counts in the same render; confirmed by re-reading those call sites before finalizing that none of them depend on `items`'s original array identity or iteration order in a way a copy would break. Test changes: the old `cr-dashboard-menu-management-link` "AC6 — renders the permanent 'No recent activity' empty state" test seeded one item and asserted the empty message — that assertion is now false on purpose (AC3 supersedes AC6), so it was rewritten (same zero-items intent, now genuinely zero items) rather than deleted or left contradicting the shipped code (B3). Three new tests added: AC1/VC-CR-001 (6 items with distinct `updatedAt` values, asserts exact rendered order and the 5-item cap, explicitly asserting the 6th/oldest item's name is absent); AC2/VC-CR-002 (one item with `createdAt === updatedAt` and one with `updatedAt > createdAt`, asserting "Added"/"Updated" on each item's own row in both directions — would fail if the logic were swapped or one label always rendered, per B2's accept/reject-symmetry requirement); AC3 (zero items, asserts `NO_RECENT_ACTIVITY_MESSAGE` still renders). `pnpm --filter @app/web build/typecheck/lint/test` all green (18 test files, 158 tests — was 156, net +2: one test replaced by three). No live browser verification performed by this builder call — no browser tool available to this role, per existing precedent in prior CR build entries; left for the orchestrating session. Pre-existing dirty `git status` at task start (scaffold/docs/`.claude` process files from prior sessions, unrelated to and untouched by this task) reported but not worked around — nothing under `platform/` was dirty, so it did not conflict with this task's file set. | PROCEED |
| 2026-08-14 | cr-dashboard-recent-menu-changes (planning) | PROCEED | User-requested: "Recent Menu Changes should show first 5 latest changes" — the card currently always renders a hardcoded "No recent activity" empty state, per `cr-dashboard-menu-management-link`'s own AC6, whose stated reason was "no activity/audit-log data source exists in this codebase." **Re-checked that premise before designing a fix, since it's the crux of whether this is even CR-sized**: `cafe.MenuItem` (`shared/contracts/src/cafe/menu.ts:125-126`) already carries `createdAt`/`updatedAt` (ISO strings) on every item, and `DashboardScreen` already fetches the full item list via `GET /menu/items` for the KPI cards (`cr-dashboard-menu-management-link` AC2) — so a real, already-in-hand data source for "recent changes" does exist, just not a full audit log (no per-field diff, no "who changed it," no delete history). AC6's "no data source" framing was accurate for a true audit feed; it's not accurate for "which items changed most recently," which the existing timestamps answer directly with zero new backend/contract work. **Amends**: `cr-dashboard-menu-management-link.md`'s AC6 (superseding its permanent-empty-state text with a real, derived list; noted directly in that CR's own text, same pattern as the `cr-session-guard-redirect-to-login`/`cr-logout-and-back-navigation` supersession). Chosen design: sort the already-fetched `items` array by `updatedAt` descending, take the first 5, render each as `<name> <added|updated>` + a formatted timestamp — `added` vs `updated` is derived by comparing `createdAt === updatedAt` (TypeORM's `@CreateDateColumn`/`@UpdateDateColumn` both stamp `now()` in the same INSERT, so a never-edited item's two timestamps are identical; any edit moves `updatedAt` forward). Zero items still falls back to the existing "No recent activity" text — now a genuine empty state, not a permanent one. No new endpoint, no new contract field, no audit-log subsystem — explicitly not attempting to fabricate "what changed" (old value vs new value) or capture deletions, since neither is derivable from the data that exists; this stays honestly scoped to "which items were most recently created or edited," which is what was asked for. `Build gate: no` — purely a client-side derivation over data already being fetched for the KPI cards; no backend/contract change, no new risk surface. | PROCEED |
