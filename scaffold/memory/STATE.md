# STATE

Where the build is right now. Read first every session; refresh every `/wrap`.
Keep it short — a pointer, not a history. The git log is the history.

---

**Current focus:** _Three of eight café-suite PRDs are shipped: `login` (T01-T11, committed), `cafe-access-roles` (T00-T03, committed), and `cafe-menu-management` (T01-T11, committed `7e9dd99`/`df75d95`). `login`'s PRD file stays in `platform/docs/prd/_ACTIVE/` rather than `_SHIPPED/` — see Open HOLDs below. `cafe-access-roles` and `cafe-menu-management` are archived to `_SHIPPED/`, their task ledgers removed (history lives in git log, `DECISIONS.md`, `build-trace.md`). Five café PRDs remain unbuilt and need wireframes before `/plan`: `cafe-tables-reservations`, `cafe-order-management`, `cafe-kitchen-display`, `cafe-billing-payments`, `cafe-inventory-management` — see each PRD's own "Depends on" line for build order._

**Last session:** 2026-08-12/13 — built `cafe-menu-management` T06-T11 (availability toggle, category reorder, the Add/Edit Menu Item forms, the admin list screen), closing the `Authorization`-header gap in `@app/frontend-core`'s `createApiClient` along the way (T10). Fixed three real process gaps found via a build-trace review: B2 now requires accept+reject test symmetry on new validation logic, the Docker/`test:int` check is capped at ~60s instead of open-ended retries, and trace-file appends must re-verify true EOF first (a mid-file insertion bug happened once and was hand-fixed). Diagnosed and fixed a real Docker Desktop failure (Windows Defender blocking removal of stale AF_UNIX socket files on every startup — not a code issue); `test:int` now runs and passes for real (42 tests, `auth-service` + `cafe-service`). Ran `/commit` and `/wrap`.

**Open HOLDs:**
- `login` — session-expiry banner (`docs/wireframes/login/index.md`'s "States not drawn") has no bound AC/task; a `DECISIONS.md` entry ("T11 (reviewer escalation)") explicitly gates this **before the PRD is considered fully shipped** — needs a planning decision (add a task once `/dashboard` has enough shape to trigger it meaningfully, or confirm out of scope and update the wireframe doc). This is why `login.md` stays in `_ACTIVE/`.
- `cafe-menu-management` — AC6's reorder-*trigger* control (drag handles / up-down arrows) has no wireframe and was never built anywhere (backend persistence and the display-reflects-order half both exist and work). Needs a wireframe before a task can be planned for it. Logged as OPEN in `DECISIONS.md` ("cafe-menu-management (planning)") — archived anyway since every currently-planned task is done; this is accepted scope, not a defect.
- Two lower-priority, non-gating notes for future café PRDs, both in `DECISIONS.md`: `cafe-access-roles` T03's `require-role-policy.cjs` lint check only catches same-*method* `@Public()`+`@RequireRoles()` conflicts, not same-*class* (not currently exploitable, revisit when a controller first uses a class-level decorator); a stale pre-single-branch-workflow note about `feature/login` not being merged to `master` (moot now that everything builds directly on `master`).

**Uncommitted work:** _Yes, in both repos._
- **Outer scaffold repo** (workspace root): 11 modified files — the `taskplanned/` path-migration edits from earlier this session (`.claude/agents/builder.md`/`planner.md`, `.claude/commands/build.md`/`commit.md`/`plan.md`, `CLAUDE.md`, `docs/BaseWorkspace_Structure_Lite.md`, `scaffold/inputs/repo-structure.md`) plus this session's three build-trace process fixes (also in `builder.md`) and `scaffold/policies/agent-policies.md`'s B2 sync, plus this `/wrap`'s own edits to `DECISIONS.md`/`STATE.md`. None of this has been committed since `b2eb7cc`.
- **`platform/`**: the PRD-archival moves from this `/wrap` (`cafe-access-roles.md`/`cafe-menu-management.md` → `_SHIPPED/`, their task ledgers removed) — everything else is committed through `df75d95`.
