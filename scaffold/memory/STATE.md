# STATE

Where the build is right now. Read first every session; refresh every `/wrap`.
Keep it short — a pointer, not a history. The git log is the history.

---

**Current focus:** _Platform scaffolded (auth + core services, portal web app, 4 shared packages). All four gates green on an empty scaffold. No features built yet._

**Last session:** 2026-08-06 — restructured from a flat monorepo into `scaffold/` + `platform/` with right-sized services. Migrated the login PRD and wireframes into `platform/services/auth/docs/`.

**Next up:** `/plan auth` — map the login PRD's 11 ACs against its 3 wireframes into `platform/services/auth/docs/task-ledger.md`.

**Open HOLDs:** _None — see [DECISIONS.md](DECISIONS.md)_

**Uncommitted work:** _None_
