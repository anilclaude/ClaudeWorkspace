// AppShell (src/components/app-shell.tsx) already provides <main> and page
// padding — pages render content only, never their own page chrome.
export default function Home() {
  return <p className="text-sm text-slate-500">Scaffold ready. No features built yet.</p>;
}
