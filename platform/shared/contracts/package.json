{
  "name": "@app/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "The single source of truth for every cross-service and client-server contract. A service that serves a route and every consumer of that route import the SAME schema, so a breaking change is a compile error rather than a production incident.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
